# P28 进度：持球行动机会核心模块（#25 阶段 2A）

分支 `p28-action-opportunity-core/2026-09-13`（立项 commit `c82cd1d`）。
方案定稿：`openspec/changes/p28-action-opportunity-core/{proposal,design,tasks}.md` +
`reviews/grill-design.md`（4 个决策已拍板）。

## 现状勘察（已确认）

- 开放比赛决策入口：`tick` 主循环 lib.rs:880-904 的 if 链
  `slot_clock ≥ slot_interval → roll_highlight` > `hold_ticks ≥ PASS_BREAK_TICKS(12) → 过渡传球`
  > `maybe_open_foul` > `emit_beat_with_main`。
- `roll_highlight`（lib.rs:1264）先 `roll_highlight_slot`（Shot35/Corner12/ThrowIn18/Tackle22/Pass13）
  再分派到既有 emit——**类型配额在前、场上动作在后**（#25 的因果倒置）。
- `roll_highlight` 的 Shot 分支内含三态：射程内直射 / >SHOT_PASS_ADVANCE_M(40m) 向前传球 /
  其余进入 `shot_setup` 带球推进（推进期间 slot 时钟暂停）。
- Tackle 分支：`same_pair` 由 `last_tackle_pair` 决定（不耗 RNG）；`far = dist > 12m || !should_tackle(rng)`
  ——`should_tackle` 因 `||` 短路**仅在 dist ≤ 12m 时消耗 1 次 RNG**；成功判定在
  `emit_tackle_highlight_impl` 内（same_pair 时不再消耗）。
- golden 双基线：`tests/golden/`(v1=P27 前冻结) + `tests/golden-v2/`(v2=当前引擎)。
  `gm_canary_seeds` 对 v2 强断言 `stream_hash` ⇒ 接线后哈希必须逐字节不变（D5/D6 的机器门）。
- 现状全绿：`cargo test`（4 passed / 7 ignored）+ `openspec validate --all --strict` 17/17。

## 接线前基线（D5 证据，2026-09-13）

- 快照：`/tmp/p28-baseline-hashes.txt`（seed 1..=100 的 `fnv1a(事件流 JSON)` + JSON 字节长度，
  debug profile，命令：`cargo test --test zz_p28_baseline_dump -- --nocapture`）。
- 接线后用同一临时测试重跑并 `diff`——**必须零差异**（逐 seed 逐字节）。
- 仓库内永久门：`gm_canary_seeds`（v2 stream_hash 强断言）+ `gm_v1_regression_counts_unchanged`。

## 实现计划

### P1 模型（lib.rs 新增，纯结构）

- `OpportunityTrigger::{NaturalDeadline, FallbackDeadline}`（D4）。
- `OpportunityReason`：开启原因 + 结算 + D1 失效原因（球权改变/死球/犯规/重开）。
- `ActionOpportunity { carrier, age_ticks, deadline_ticks, reason }`（D1）。
- `CarrierAction::{Dribble, Pass { target: Option<i32> }, Shoot}`；`DefensiveAction::{Tackle, Foul, Contain, Jockey, None}`；
  `ActionResolution::{CarrierAction(..), InterruptedByTackle, InterruptedByFoul, DefensiveContainment, DefensiveJockey, NoAction}`。
- `compute_action_deadline(danger, pressure, escape_space)` 纯函数 +
  `action_deadline_for(st)`（几何量从 MatchState 提取，RNG-free）。
  常量：BASE 7 / DANGER_URGENCY 4 / PRESSURE_URGENCY 3 / ESCAPE_BONUS 5 / clamp[3,12]。
  （ESCAPE_BONUS=5 使上界 12 可达：7-0-0+5=12；下界 7-4-3+0=0→clamp 3。）

### P2 决策函数（2A 包装旧逻辑）

- `evaluate_carrier_action(st, rng, trigger) -> CarrierPlan`
  - `FallbackDeadline`：**包装** `roll_highlight_slot` 的抽签 → CarrierAction（不改抽签、
    不改 RNG 次序）；Shot 槽内 `sample_shot_target` 与三态判定原样保留。
  - `NaturalDeadline`：RNG-free，返回 `Dribble`（2A 等价门：执行入口仍是既有涌现机制）。
- `evaluate_defensive_action(st, rng, &carrier_plan) -> DefensivePlan`：仅 Tackle 槽/非门将时
  按旧次序算 `same_pair`/`far`（含 `should_tackle` 短路语义），其余返回 `None`（不耗 RNG）。
- `resolve_action_opportunity(&carrier, &defensive) -> ActionResolution`：D3 优先级
  死球/重开 > 防守中断(tackle/foul) > 持球终结(shoot) > 持球普通(pass/dribble) > contain/jockey > NoAction。
- **2A 执行绑定** `CarrierExecution`/`DefensiveExecution`：把结算映射回原 emit，保证逐字节等价
  （2B/2C 起由真实 hazard/竞争替换执行层）。

### P3 接线

- `tick` 开放比赛分支：`slot_clock ≥ slot_interval` → `open_action_opportunity(FallbackDeadline)`
  → 评估 → 结算 → 执行（原 `roll_highlight` 的分派职责迁入 `execute_action_resolution`）。
- `PASS_BREAK` 分支 → 评估为 `CarrierAction::Pass { allow_out: false }` 后执行同一执行层。
- 默认分支：自然 deadline 生命周期（开机会 / `age_ticks` 推进 / 到期评估→重置，结算恒为
  「继续带球」→ `emit_beat_with_main`，零 RNG）。
- 失效：死球 / 重开 / 高亮 / 松散球 / 射门推进 / 犯规 tick 一律 `invalidate_action_opportunity(..)`；
  机会的 `carrier` 与 `st.carrier` 不一致时按「球权改变」失效。
- `MatchState` 新增 `action_opportunity` + `opportunity_tally`（**只写不计入事件流、不耗 RNG**，
  供「新模块真被调用」守卫观察）。

### P4 测试

- deadline 纯函数：近门/受压 → 短；后场无压 → 长；边界 [3,12]；门将持球 → 偏长。
- 结算优先级：tackle/foul 压 shoot；shoot 压 pass；pass 压 contain；全空 → NoAction。
- 等价性：`gm_canary_seeds`（v2 stream_hash）+ `/tmp` 100 seed 指纹 diff 零差异。
- **非空转守卫**：`action_opportunity_wiring_is_live`——跑满一场（5400s）断言
  fallback/natural 机会数 > 0、CarrierAction/DefensiveAction/ActionResolution 各分支均被命中、
  且「执行绑定计数」与事件流中对应事件数一致（把新模块计数与事件流绑死：绕过模块 → 计数 0 → 红）。

### P5 收尾

- `./verify.sh` 全绿 + `npx openspec validate --all --strict` 17/17。
- 独立零记忆 paseo subagent 审阅闭环（D5 等价 / 非空转 / fallback 隐性配额三项为重点）。

## 已知解释（需在审阅中明确，非放宽 D5/D6）

2A 被禁止改变可观测行为（协议/统计/golden 全不变），因此新增机制在 2A 内**不可能**产生新事件；
D5 的落法是「结构真接、行为等价」：fallback 路径上新模块是**真承重**（删除它 → 无事件可产），
自然 deadline 路径在 2A 内的结算恒为「继续带球」，其可见证据是 tally（2B 起几何/竞争真正生效）。
这一解释在实现后写入 `.p28-progress.md` 与审阅材料，不自行放宽 D5/D6。

## 进展

- [x] 勘察 + 基线快照
- [ ] P1 模型
- [ ] P2 决策函数
- [ ] P3 接线
- [ ] P4 测试
- [ ] P5 收尾（verify + 审阅闭环）
