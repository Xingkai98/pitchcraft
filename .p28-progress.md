# P28 进度：持球行动机会核心模块（#25 阶段 2A）

分支 `p28-action-opportunity-core/2026-09-13`（立项 `c82cd1d`）。
实现 commit：`71b884c`（P1-P4）+ `8875f52`（P5 spec 对齐）。
方案定稿：`openspec/changes/p28-action-opportunity-core/{proposal,design,tasks}.md` +
`reviews/grill-design.md`（4 个决策已拍板）。

## 实现落法（继承前任勘察结论）

- 开放比赛统一走 **开机会 → 评估 → 结算 → 执行**：`tick` 的槽位分支 / 持球超时分支 / 普通
  tick 三条路径都经由 `build_action_plan` + `execute_action_resolution`。
- **fallback 路径真承重**：`roll_fallback_situation` 抽**情境**（1 次 RNG，与旧
  `roll_highlight_slot` 同值同序）→ `evaluate_carrier_action` 映射候选行动（`sample_shot_target`
  与三态分流原样保留）→ `evaluate_defensive_action` 按旧序算 `same_pair`/`far`（含
  `should_tackle` 的 `||` 短路语义）→ `resolve_action_opportunity` 走 D3 优先级 →
  `execute_action_resolution` 落回既有 emit。
- **自然 deadline 路径在 2A 内决策中性**：`evaluate_carrier_action(NaturalDeadline)` 零 RNG、
  不承诺行动；结算恒为 `NoAction`/`Containment`/`Jockey` → `emit_beat_with_main`。可见证据是
  tally 与 deadline 几何；2B 射门 hazard 接管该路径。
- **tally 只写、不进事件流、不耗 RNG**；执行绑定计数与事件流对应事件数绑死。

## 测试门槛（全绿）

- `./verify.sh` 全绿（Rust 72 passed / realism 4 passed+7 ignored / viewer 304 / tools 400 /
  WASM e2e / L1 release 7 passed）。
- `npx openspec validate --all --strict` 17/17。
- 纯函数测试：deadline 公式方向+边界+钳制、结算优先级、D3 一致性、几何方向+门将放宽、
  RNG 序同构、自然 deadline 零 RNG、实测 deadline 范围。
- **非空转守卫** `p28_action_opportunity_is_live`：20 场，断言触发/候选/结算/失效各分支覆盖、
  执行绑定计数逐场 == 事件流事件数、`opportunity_leaks == 0`、
  `natural_deadline_due == res_containment+res_jockey+res_no_action`。

## D5 行为等价证据（逐字节）

- 基线：`/tmp/p28-baseline-frozen.txt`（seed 1..=100 的 `fnv1a(JSON)` + 字节数，接线前快照）。
- **seed 1..=100**：接线后重跑 `diff` **零差异**。
- **seed 1..=300**（独立副本 `/tmp/p28-ext`，`git archive` 双版本对跑）：**零差异**。
- **多时长 + demo**：dur ∈ {300,600,1800,3600,5400} × seed 1..=40 + demo seed 1..=20：**零差异**。
- 仓库内永久门：`gm_canary_seeds`（v10 seed，v2 stream_hash 强断言）+ `gm_v1_regression_counts_unchanged`。

## 绕过守卫验证（防空转，独立副本实测）

在 `/tmp/p28-ext` 副本里把槽位分支改回旧 `roll_highlight` 式直接分派（完全绕过模块）：
- 事件流仍与接线前 **300 seed 零差异**（确认该绕过是「行为等价」的）；
- 但 `p28_action_opportunity_is_live` **变红**（`exec_shoot` 计数 0 vs 事件流 shot 数 6）。
→ 守卫确实绑定在模块执行上，而非事件本身；P27 `model_version` 式空转会被抓住。

## 已知解释（非放宽 D5/D6）

2A 被禁止改变可观测行为，因此新增机制在 2A 内不可能产生新事件。D5 的落法是「结构真接、
行为等价」：fallback 上模块真承重（绕过 → 守卫红），自然 deadline 决策中性（2B 起生效）。
- `CarrierAction(Dribble)` 结算在 2A 结构性不可达：唯一 Dribble 候选来自 fallback 被逼抢情境，
  该情境必伴随防守候选 Tackle → D3 优先级下恒为 `InterruptedByTackle`。守卫断言其为 0 并
  单独覆盖候选层，不谎报覆盖（2C 接触竞争后可达）。
- `opportunity_leaks` 恒 0：球权改变/死球/重开在接线里必先经高亮，高亮起点已即时失效；
  这三条退化为「不漏」防御网。`invalidated_foul` / `invalidated_play_broken` 为实际触达路径。

## 审阅闭环

### 第一轮（paseo 独立零记忆 agent `7c543760`）：**通过**，无 P0/P1，5 条 P2

独立复现：100 seed + 加宽 180 场（dur×seed + demo）指纹**零差异**；绕过模块变异 → 守卫红
（`exec_shoot 0 vs 6`）；删高亮失效 → `opportunity_leaks 269` 红；300 seed 探针
`leaks=0 / unattributed=0`；1500 seed fuzz 无 panic；`cargo test` 72/72、realism L1 7/7、
openspec 17/17。

P2 修复：
1. deadline 下界 3 实战不可达（danger 三因子连乘）→ 加注释澄清「钳制区间」≠「3 常现」。
2. `assert_resolution_consistent` + reason 不变量 `debug_assert` → `assert!`（release 也守）。
   已验证 release lib p28 8/8 + realism L1 7/7 无误 panic。
3. `defensive_none` 只写不读 → 守卫新增 `> 0` 断言 + 说明覆盖口径。
4. 删除临时测试 `zz_p28_baseline_dump.rs`（证据落 /tmp + 本文件；仓库内门 = golden canary）。
5. `.p28-progress.md` 随本轮提交。

### 第二轮：进行中

## 进展

- [x] 勘察 + 基线快照
- [x] P1 模型 / P2 决策函数 / P3 接线 / P4 测试
- [x] P5 verify + spec 对齐
- [x] 第一轮审阅 + P2 修复
- [ ] 第二轮审阅（进行中）
