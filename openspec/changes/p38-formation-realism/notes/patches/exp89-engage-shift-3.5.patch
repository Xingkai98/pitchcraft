# P38 #89：★ 射门倾向 -4.3→-3.5：L1 口径射门 7.75→15.71/场（真实 16.51）、犯规 23.46 ∈ L1[16,30]；但进球 0.89→2.11，L1 **6 绿 3 红**（shot_result / tackle_dilution / home_away）
# 变体名 engageShift-3.5（probes-main/exp89-variants.mjs）
# 干净 main = 901da77b
--- a/engine/src/lib.rs	2026-09-20 18:34:32.530395905 +0800
+++ b/engine/src/lib.rs	2026-09-20 18:34:32.534395936 +0800
@@ -553,7 +553,7 @@
 const OPEN_PLAY_PASS_PRESSURE_M: f64 = 8.0;
 /// 射门推进档的 hazard 平移（log 尺度）：把 2B 的射门五因子从「起脚窗口内该不该射」平移到
 /// 「开放机会点上该不该启动一次推进」。**离线校准参数**（design D1）。
-const OPEN_PLAY_SHOT_ENGAGE_SHIFT: f64 = -4.3;
+const OPEN_PLAY_SHOT_ENGAGE_SHIFT: f64 = -3.5;
 
 /// 起脚窗口压迫桶下标（0=贴身 / 1=无压 / 2=中间），零 RNG。
 fn shot_pressure_bucket(nearest_defender_m: f64) -> usize {
