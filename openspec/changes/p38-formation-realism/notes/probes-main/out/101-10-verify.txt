# #101 探针 10：独立复核（另一套实现）

样本：SkillCorner 前 **2** 场（复核用小样本，独立实现）。

帧·队：**8,772**

## 10.1 两条自指恒等式（全量面板，逐行核对）

| 恒等式 | 行数 | 最大绝对误差 | 判定 |
|---|---|---|---|
| `nearMateGap + nearMateY ≡ y` | 879,080 | **7.105e-15** | ✅ 精确成立 |
| `nearOppGap + nearOppY ≡ y` | 879,080 | **7.105e-15** | ✅ 精确成立 |

> 7.1e-15 ≈ 双精度在 ~70m 量级上的舍入极限（约 1e-14 相对精度），即**代数恒等**。

## 10.2 置换零分布（独立实现，邻居表与 R² 全部另写）

| | k3MateY 的 R² |
|---|---|
| 真实 | **0.649** |
| 零分布（8 次重排） | 0.0005, 0.0008, 0.0004, 0.0008, 0.0005, 0.0005, 0.0005, 0.0008 |
| 零分布均值 | **0.00060** |
| 分离比 | **1081×** |

> 与探针 6（20 场全量、20 次重排）的结论一致：真实远高于零分布。
> 注意两套实现的**绝对水平不同**（本探针只用前 2 场、8 次重排），这是样本量差异，不是矛盾。
