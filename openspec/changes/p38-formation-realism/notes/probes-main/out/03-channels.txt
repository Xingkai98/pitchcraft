样本：Metrica 2 场 + SkillCorner 6 场；引擎 1 种子（对照）。全点口径（恒 10 人/帧）。

## Q3a. 个体 y 分布的形状

逐人（一场一个身份槽位，n≥300 帧）算：sd、IQR、全距；峰性 = 2m 桶最大占比 ÷ 均匀占比
（=1 表示在 68m 宽上完全均匀，=34 表示全部挤在一个 2m 桶）。kurt 是峰度（正态 = 3）。

         数据集      人·场 n    y sd(m)   y IQR(m)     y 全距(m)        峰性        峰度
────────────  ─────────  ─────────  ─────────  ──────────  ────────  ────────
     metrica         40      12.46      17.32       68.47      2.72      2.96
 skillcorner        120      12.87      18.85       66.50      2.38      2.62
      engine         20       0.76       0.62       12.04     26.49     47.36

**读法**：真实球员的整场 y sd ≈ **12.5–12.9m**，与"68m 宽上均匀随机游走"的 sd≈19.6m 只差 1.5 倍；
引擎 **0.76m**。差距 **~17×**。峰性 2.4–2.7（远小于引擎的 26.5）= 真实 y 分布**几乎没有尖峰**，
峰度 2.6–3.0 接近正态。**"每个球员有自己固定的横向通道"在分布形状上不成立**——
若成立，每人的 y 分布应是一个窄峰（峰性会高一个数量级），实测不是。

> 注：本表的 sd **已做半场朝向归一**（`yCanon`，见 `00-caliber.txt` §3b）。
> 未修正的原始 y sd 是 14.2 / 15.3m——那多出来的 1.7–2.4m 是"半场换边把球员搬到了另一侧"，
> 不是他的跑动。这是本票据修掉的口径 bug。

### ⚠ 与 P38 既有数字的对账（口径不同，两个都对）

`findings-final.md` §4b 的"真实横向 sd **10.04m** vs 引擎 **0.53m**"是**窗内**口径
（120s 窗 t∈[1800,1920)，Metrica game1）。该窗完全落在上半场（边界 2850s）内，
故朝向归一不影响它——**10.04m 逐位复现**。本表 12.5–12.9m 是**整场**口径，
两者差在**慢漂移**（整场重心/比赛态势），不是矛盾。窗内复算：

            数据集/场次      窗内帧 n      人数       窗内 y sd 均值(m)
──────────────────  ─────────  ──────  ──────────────────
         metrica 1        600      20               10.04
         metrica 2        600      20               10.07
skillcorner 1874553        365      20                9.28
skillcorner 1886347        489      20                8.77
skillcorner 1899585        241      20                9.60
skillcorner 1959846        452      20                7.07
skillcorner 2007448        346      20                8.37
skillcorner 2013725        435      20                8.87
 engine engine-s42        600      20                0.53

引擎 0.45–0.70m、真实 7.1–10.1m（SkillCorner 因每帧人数少、窗内有效样本更少，
  见 00-caliber；其窗内 sd 系统性略低）。**窗内比值 ~13–22×**，与整场比值一致。

## Q3b. 个体 y 的通道占用（5 条 13.6m 通道）

先与 `real-formation-laws.md` §6.2 对账（全员占比），再给**逐人的主导通道**：
每人最常出现的通道及其占比（"主导通道%"）——若 ≈100%，说明此人**几乎不换通道**。

         数据集        人次 n       左翼       左半       中路       右半       右翼            主导通道占比均值
────────────  ──────────  ───────  ───────  ───────  ───────  ───────  ──────────────────
 skillcorner     2604000    11.9%    21.7%    33.0%    21.7%    11.8%               38.6%
     metrica     1144680    10.4%    21.7%    34.9%    22.7%    10.3%               42.1%
      engine      540020     6.0%    29.3%    31.1%    29.6%     4.0%               82.9%

（`real-formation-laws.md` §6.2 的 SkillCorner 全员占比：12.3% / 22.4% / 33.8% / 21.1% / 10.4%。
  本表用全点口径 + 逐人先算，故有细微差异；方向应一致。）

## Q3c. "主导通道"是稳定的身份还是随机波动？

把每人的**前半场 y 中位数**与**后半场 y 中位数**对比（同一人、同一场），并把
这个差拆成**队伍分量**（该队前后半场整体重心的差）与**个体分量**（残差）。
若侧向职责是硬约束 → 个体分量应远小于队伍分量；若"通道"只是跟着球走 → 两者同量级。
另给**打散对照**（每人 y 序列随机重排后分两半）作"无结构"基线。

         数据集      人·场 n        |前后半场 y 中位差|         其中队伍分量        个体分量        打散对照
────────────  ─────────  ──────────────────  ─────────────  ──────────  ──────────
     metrica         40                6.67           2.13        6.33        0.17
 skillcorner        120                4.73           2.16        4.04        0.18
      engine         20                0.03           0.01        0.05        0.00

**读法**：个体分量 ≪ 打散对照 → 同一人在**同一场内**确实守着相对稳定的横向档位；
个体分量 ≈ 队伍分量 → "通道"主要是全队横移的投影，不是个体职责。
（注意：这里的"前后半场"是**整段 45 分钟**。若中位差很大，说明半场之内 y 就在漂移，
  而不是"球员守一个固定档位、只在其附近抖动"。）

## Q3d. y 中心 ↔ 真实位置（SkillCorner position_group）

每组：人数、y 均值（距中轴）、y sd、跨人 sd（组内不同人的 y 中心有多分散）。

        position_group      人·场 n     y 均值(m)       距中轴(m)    人内 y sd(m)     跨人 y 中心 sd(m)
──────────────────────  ─────────  ──────────  ───────────  ────────────  ────────────────
        Center Forward         19       32.92        -1.08         13.67              3.75
      Central Defender         26       34.41         0.41         10.55              6.78
             Full Back         24       33.64        -0.36         11.91             15.43
              Midfield         27       33.83        -0.17         13.81              3.93
         Wide Attacker         24       34.80         0.80         14.67              9.42

**读法**：`跨人 y 中心 sd` 是本表的关键——它衡量"位置真的决定了横向档位吗"。
⚠ **不要把它读成"球员习惯差异"**：这里的"人·场"里，**同一个槽位在不同场次是不同的人**，
而且同组内**左/右两名球员被算作两条记录**。所以：

- **Full Back 15.43m** 不是"边卫各有习惯"，而是**左后卫（y≈14）与右后卫（y≈54）的分离**——
  它是"边卫确实有左右分工"的**直接证据**，量级等于场地宽的一半。
- **Central Defender 6.78m** 同理（左中卫/右中卫的间距），但小得多；
  **Center Forward 3.75m / Midfield 3.93m** 最小 → 中锋与中场**没有左右分工**。
- 因此正确的结论是：**左右分工只存在于"边后卫 + 边锋"这类边路位置，且是"相对进攻方向"的**
  （本表已做朝向归一，否则左/右会被半场换边搅成一半一半）。

「人内 y sd」一列则给出各自的活动半径：Full Back 11.9m 最小（沿边线上下，横向约束最强）、
Wide Attacker 14.7m 最大。全部 10.6–14.7m，与 Q3a 的 12.5–12.9m 一致。

### 逐场版本（同一场、同一位置组内的横向分散）

每组在每个**单场**里的成员 y 中心的全距（max − min）与 sd，跨场取均值。
这是"位置是否强制左右分工"的干净读数：≤3m = 该组的人都在一条横轴上；
≥10m = 组内至少有一对左右分居两翼。

        position_group      场数       组内人数       组内 y 全距均值(m)     组内 y sd 均值(m)      全距中位(m)
──────────────────────  ──────  ─────────  ─────────────────  ────────────────  ───────────
        Center Forward       6        3.2               6.73              2.90         6.74
      Central Defender       6        4.3              16.27              6.50        14.15
             Full Back       6        4.0              33.54             15.42        33.61
              Midfield       6        4.5               9.69              3.82         9.99
         Wide Attacker       6        4.0              21.31              8.98        20.97

## Q3e. 个体 y 分布是"双峰"（两侧各一个通道）还是"单峰宽分布"？

对每个「人·场」序列，看 y 直方图（1m 桶）有几个显著峰。也分左右两侧统计
（左半边 / 右半边各自的占比）——若明显偏离 50/50 且呈双峰，则此人有**左右两个职责档位**。

         数据集      人·场 n          有 ≥2 个显著峰的比例            左右占比偏离 0.5 的均值
────────────  ─────────  ────────────────────  ────────────────────────
     metrica         40                 95.0%                     0.198
 skillcorner        120                 98.3%                     0.204
      engine         20                  0.0%                     0.429

**读法**：`左右占比偏离 0.5` 小 = 该球员在场上的左右两侧**时间大致对半** → 说明他是"跟着球换边"的；
大 = 他基本固定在某一侧（真的边路职责）。

⚠ **"≥2 个显著峰"这一列会误导**（实测 95–98% 的真实球员都 ≥2 峰，引擎 0%）。
原因是 1m 桶 + ±1 平滑下，一个**宽而平**（sd 12m、接近正态）的分布本来就有一堆
统计噪声峰；它不是"左右两个职责档位"的证据。**看后一列**（左右占比偏离）与 Q3d 的
逐场分散才是判据。

后一列的读法：真实 0.20 左右（不是 0、也不是 0.43）→ 球员**约 70:30 偏向某一侧**，
既不是"严格守一侧"（那会到 0.4+）也不是"完全随球换边"（那会到 0）。
引擎 0.43 = 极端的固定一侧（它的球员 y 几乎不动，随机哪边就一辈子哪边）。