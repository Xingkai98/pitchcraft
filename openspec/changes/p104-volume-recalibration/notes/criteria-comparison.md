# P5.6 判据组 8 条逐条对照（**如实记录，不预设「不恶化」**）

> 用户第 2 轮决定 5（M5）：把原「判据组不恶化」改成「逐条对照并如实记录」——
> `swarm` 确实恶化了。本文件是实测结果。
> 跑法：`node openspec/changes/p38-formation-realism/notes/criteria/check-criteria.mjs`
> （**报告期、不阻塞**；verify.sh 第 9 步）。wasm：干净 main `901da77b` / 候选读数所属构建 `b12fa58c`（见 `design.md` §D0）。

| 判据 | 目标带 | 干净 main | 候选 | Δ | 判定 |
|---|---|---|---|---|---|
| 纵向 纵深 hd | [23.4, 28.4] | 40.32 ❌ | 40.14 ❌ | −0.18 | 不变（都远未达标，现状） |
| 纵向 紧凑度 spread | [13.4, 17.0] | 18.62 ❌ | 18.67 ❌ | +0.05 | 不变 |
| 纵向 重心间距 gap | [6.5, 10.1] | 23.46 ❌ | 23.25 ❌ | −0.21 | 不变 |
| 横向 横向位移 sd latSd | [7.0, 14.0] | 0.553 ❌ | **0.625** ❌ | +0.07 | 不变（都远未达标） |
| **整体性 y 两两相关 swarm** | [0.45, 0.89] | **0.488 ✅** | **0.338 ❌** | **−0.150** | ⚠️ **恶化：由达标转为不达标** |
| 结构 断层幅度 fault | [2.5, 7.5] | 19.2 ❌ | 18.86 ❌ | −0.34 | 不变 |
| 结构 各线均衡 midBack | [0.84, 1.28] | 0.832 ❌ | **0.856 ✅** | +0.024 | 翻绿（贴门槛，噪音级） |
| 行为 普通射门/场 shotsReg | ≥5.5 | 7.2 ✅ | **17.6 ✅** | +10.4 | 仍达标（体积抬升的副产品） |
| **合计** | | **2/8** | **2/8** | | |

## 读法（如实）

1. **`swarm` 确实恶化**（0.488 ✅ → 0.338 ❌），幅度 **−0.150 = 门槛间隙的 2.7 倍**
   （距下界 0.451 从 +0.037 变成 −0.113）。**这不是噪音**——
   grill 扩到 20 种子复核：干净 main 0.479 vs 候选 0.380，**回归是实的**。
   机制解释：射门/抢断变密 → 球员跑动更多被球牵引 → 队形 y 相关性下降。
   ⚠️ 与 `notes/visual-check.md` §3 一致：候选轨迹**略直、横向 sd 略降**。
2. **`midBack` 由 ❌ 翻 ✅**（0.832 → 0.856，门槛 0.84）——
   ⚠️ **这是噪音级翻面，不是本 change 挣来的**：离门槛只有 0.016，且本票**不碰队形**。
   （P38 `conversion-adaptive.md` §7 记过同类：`midBack` 在变体上 0.832 → 0.842 恰好翻绿。）
3. **其余 6 条原地不动**（最大 |Δ| = 0.34，在种子噪声内）——
   与 P38 §2.2「队形与体积在两根独立轴上」一致。
4. **合计 2/8 → 2/8**（`swarm` 掉一条、`midBack` 捡一条，**互相抵消**，
   但两条的性质完全不同：前者是**真回归**，后者是**噪音**）。

## 判定

- 判据组是**报告期、不阻塞 verify.sh**，故不影响本票的交付判定。
- ⚠️ **本 change 对队形判据是「净中性偏负」**：真回归一条（`swarm`）、
  真改善零条、噪音翻绿一条。**如实记录，不把它算作「未恶化」。**
- 这**符合预期且不构成本票的缺陷**：队形是 #89 的账（P38 `findings-final.md`），
  本票只做体积，且**没有让任何几何判据变成新的不达标**（6 条原地不动）。
