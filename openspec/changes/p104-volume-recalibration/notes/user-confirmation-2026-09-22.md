# 用户拍板记录（2026-09-22，主 session 转达）

> **用户原话：「按你推荐」**——即 Q1–Q6 全部采纳 agent 的建议，但 **Q2 的锚点按主 session
> 独立复核的结论修正**（见下）。本文件是**决策输入**，请把内容记进 `design.md` 的
> `## User Confirmation`，并据此更新 `specs/match-engine/spec.md` 去掉所有 `[待确认]`。

## 决定

| # | 决定 |
|---|---|
| Q1 | **3 常量**（方案 ③）：`OPEN_PLAY_SHOT_ENGAGE_SHIFT −3.4` + `BASE_DEF_TACKLE −0.65` + `BASE_DEF_FOUL −0.06` |
| Q2 | 射门带锚 **StatsBomb 大五联赛子集 ~21.7**，带 = **`[17, 26]`** ← **不是** design 里建议的 `[13,21]` |
| Q3 | 主队进球带 **`[0.85, 1.60]`** |
| Q4 | 角球单场上界 **`≤18`**（但见下方「需你核实」②，可能应改 `≤15`） |
| Q5 | **确认**：`MODEL_VERSION` 6→7 + 新建 `engine/tests/golden-v7/` |
| Q6 | **确认**：重生成 `viewer/data/benchmark-baseline.json` |

## ⚠️ Q2 锚点：主 session 独立复核推翻了 design §D7-Q2 的表述

design §D7-Q2 写的是「真实侧两个来源分歧……**本票没有定论**」。**这个表述不准确。**

主 session 独立重算了 StatsBomb 356 场（脚本与结论在 **`.scratch/anchor-verify/`**，含 README）：

1. **两个数不冲突，是 Metrica 的小样本噪声**：StatsBomb 逐场分布 均值 **22.37** / sd **7.04**。
   从该分布随机抽 **2 场**、均值 ≤ 17.5 的概率 = **13.7%**。
   Metrica 的 16.51 落在第 **23 百分位**——2 场样本的正常落点，**不构成对 22.4 的反驳**。
2. **不是赛事构成造成的**：按赛事拆解，**西甲（Metrica 自己的联赛）115 场 = 21.76**、
   **大五联赛 170 场 = 21.64**。同联赛对照仍是 ~22，排除「世界杯/女足抬高均值」的解释。
3. **不是口径 bug**：StatsBomb `body_part` 字段 **100% 完整**（前 80 场 2223 脚射门，0 缺失），
   头球识别干净。唯一确实的口径差是「只算 Open Play」= **20.41**
   （剔点球 0.48 + 任意球 1.47），**仍远高于 16.51**。

→ 用户据此拍板：**锚 StatsBomb 大五联赛子集 ~21.7，带取 `[17, 26]`**（最保守、最可辩护的子集）。

**请把 §D7-Q2 改成上述事实**（引用 `.scratch/anchor-verify/`），
spec delta 的射门带 `[13,21]` → **`[17,26]`**，去掉 `[待确认]`。

### ⚠️ 一个必须如实处理的后果

锚 21.7 后，候选 **17.09 落在带的下界附近**（不是正中靶心）。
spec 里要**如实写明**：「当前体积在真实值的下沿、但已在带内」。

**不要**为了好看把下界压到刚好装下 17.09——那正是本 change 要消除的「按现状标定」，
是 #91 判据组反复抓的那类「把带撑到刚好装下」。

若你认为 17.09 贴下界不可接受、想继续抬体积到 ~21：**先停下来把取舍报给主 session**，不要自行决定。

## 需你核实/处理的两条（同类问题）

### ① 抢断带 `[22, 40]` 的锚点同样要复核

design 里写「真实：WhoScored 两队 15–17/队 → ~30；StatsBomb `Duel/Tackle` 356 场 37.3」。
请按 Q2 的同一方法把两个来源的**口径**对齐后再定带：

- StatsBomb 的 `Duel/Tackle` 与引擎的 `tackle` 事件**是不是同一个口径**？
  （`Duel` 含不含争顶/五五开球？`Tackle` 是否只算成功抢断？引擎的 `tackle` 事件含不含失败的？）
- 在 design 里写明**锚在哪个来源、为什么**。**别直接取中值了事。**

### ② 角球上界 `≤18` 缺乏独立依据

design 写「原上界 12 = 旧均值 2.15 的 **5.6×**；新均值 3.22 的等比例上界 = **18**」。
这是**按比例外推**，不是实测依据——它没有独立理由，只是让候选刚好装得下。

请核实：18 有无**独立**理由（例如「仍远低于真实均值 ~10 的量级护栏」）？
**若没有，改用你自己列的保守替代 `≤15`**（实测 600 场 max=13，留 2 的余量）。

## 流程

1. 更新 `design.md`（含 `## User Confirmation`）+ `specs/match-engine/spec.md`，去掉所有 `[待确认]`
2. `npx openspec validate p104-volume-recalibration --strict` 通过
3. **确认 grill 落盘**：`reviews/grill-design.md` 目前**还是空的**。
   你 spawn 的 grill 子 agent（`P104 design grill`）正在跑 8 窗口 × 200 场的独立复算
   （`.scratch/grill104/`，含 `gates.mjs` / `corners-list.mjs` / `passive.mjs`）。
   **等它落盘后再进 P2**——不要跳过独立审阅
4. 按 `tasks.md` 往下实施：引擎常量 → 门重标 → golden-v7 → 主 spec → verify.sh → **看图** → 审阅闭环

## 不变的硬约束

- 引擎侧任何数字**必须当场从本 worktree 重建的 wasm** 验证（sha 记进产物）
- **看图为必须**（`notes/probes-main/render-trajectories.mjs`）——
  本 change 改了体积（射门 ×2.2、抢断 ×3.3），画面必须复核，不能只看数字
