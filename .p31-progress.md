# P31 进度：删槽位 + 涌现频率 + liveness guard（#25 阶段 3 收官）

分支 `p31-emergent-frequency-liveness/2026-09-14`（立项 `9f1c96a`）。
方案定稿：`openspec/changes/p31-emergent-frequency-liveness/{proposal,design,tasks}.md` +
`reviews/grill-design.md`（D1-D5 已拍板）；spec delta `specs/match-engine/spec.md`。

## 勘察结论（动手前）

- **槽位层是唯一的节拍源**：`slot_hold_max(5400) = 5400/24 - 4 = 221` tick；而
  `PASS_BREAK_TICKS=12` 会在 carrier 连续持球 12 tick 后强制产一次过渡传球。
  实测 90 分钟每场 **pass 事件 400.8**——其中绝大多数是过渡传球（5400/12 ≈ 450），
  24 个槽位只贡献 ~24 个高亮。即「频率」目前由**两个时钟**共同决定（槽位 + PASS_BREAK）。
- 槽位是**唯一的射门来源**：`FallbackSituation::Shot`（35%）→ `shot_setup` → 起脚窗口。
  `natural_deadline` 路径 2A 起是**决策中性**的（`CarrierPlan{action: None}`）——
  删掉 `FallbackDeadline` 后，如果自然 deadline 仍产 `None`，射门/传球会**结构性塌缩到 0**。
  → 自然 deadline 的持球决策必须变成**真实几何决策**（这是 D1「评估持球/防守」的落点，
  design 未给公式，见下方「D1 实施决策」）。
- 槽位是**唯一的普通传球出界来源**（`emit_pass_out_play_slot` + `emit_pass_highlight_inner`
  里的独立 `out_roll` 8-10%）。删槽位后出界必须由 D2 的落点误差涌现。
- `emit_pass_out_play_slot` 还有第二个调用点：起脚窗口「背向球门」分支
  （`CarrierAction::AwardDeadBall` → `CarrierExecution::PassOut`）。该分支自述**结构性不可达**
  （推进只沿进攻 x 正向、cos 恒 > 0），但其唯一执行器就是被删的 `emit_pass_out_play_slot`。

## 现状基线（P30 引擎，200 seed × 90min，release 实测）

```
进球 主0.51/客0.53 (H/A 0.962)  pass_evt=400.8  shot=6.88(+header 1.02)
tackle=5.60(succ 2.77)  corner=3.71  throw_in=3.96  goal_kick=5.04
foul=22.68(yellow 2.88/red 0.12)
```

L1 现有带（**射门/进球/犯规/传球成功率等带保留不放宽；角球与 shot/tackle 比按 D6 重标定并记录依据**）：
射门/场 ∈ [6,11]、禁区内占比 ∈ [45%,65%]、shot/tackle ∈ [1.0,1.8]、角球/场 ∈ [2,9]、
传球成功率 ∈ [0.82,0.90]、犯规/场 ∈ [16,30]、黄 [2.0,5.0]、红 ≤0.8、
主/客进球 > 1.08、主队进球/场 ∈ [0.38,0.75]、客队 ≥ 0.30、射正率 [28%,39%]、
转化 [8%,14%]、禁区内进球占比 [72%,92%]。

## D1 实施决策（design 未给公式，由本 change 补；已记录，供审阅复核）

**自然 deadline 的持球决策**（`evaluate_open_play_carrier_action`，三档，零配额）：

1. **出球档**：最近防守者贴身（≤ `OPEN_PLAY_PASS_PRESSURE_M`）→ 普通传球（`allow_out`，
   经 D2 的 `pass_risk` 调制出界通道）。「球权流动」由**被逼抢**这一状态涌现，不由时钟配额。
2. **射门推进档**：未受压且**射门推进 hazard 命中** →（复用 2B `compute_shot_score` 五因子，
   整体平移 `OPEN_PLAY_SHOT_ENGAGE_SHIFT` 到「机会点上要不要启动一次推进」的尺度）：
   `dist > SHOT_PASS_ADVANCE_M` → 长传推进；已在采样目标距离内 → 进起脚窗口；否则带球推进。
   **射门的唯一生产者不变**（仍只由起脚窗口 hazard 提交）。
3. **带球档**：其余 → 继续带球（前插倾向受 liveness guard 调制，D3）。

`OPEN_PLAY_SHOT_ENGAGE_SHIFT` / 落点误差系数 / liveness 三档幅值均为 **design D1 明说的
「离线校准参数」**，标定目标 = 上表现有 L1 带（不放宽断言）。

**删干净**：`emit_pass_out_play_slot` 被删后，其唯一执行器 `CarrierExecution::PassOut` +
`DeadBallKind` + `CarrierAction::AwardDeadBall` + `ActionResolution::DeadBall` 一并删除
（槽位时代遗留；起脚窗口「背向球门」分支改为**普通传球**——D2「禁止 Shoot 候选」仍有落点）。
`hold_ticks` / `hold_max` / `match_duration`（槽位专用）一并删。
出界的「执行绑定」计数 `exec_pass_out_corner/throw_in` 重绑到**落点误差产生的出界 pass**
（`result=out && lead.is_some()` 唯一生产者 = `emit_pass_highlight_inner`）。

## 实现计划（P1-P5，先测试后实现）

### P1 删槽位层
- 删 `HIGHLIGHTS_PER_MATCH` / `SLOT_HOLD_MIN_TICKS` / `SLOT_AVG_EVENT_TICKS` / `slot_hold_max`
  / `PASS_BREAK_TICKS` / `FallbackSituation` / `roll_fallback_situation` /
  `OpportunityTrigger::{FallbackDeadline,HoldTimeout}` / `OpportunityReason::SlotFallback` /
  `CarrierPlan.situation` / `MatchState.{slot_clock,slot_interval,hold_ticks,hold_max,match_duration}`。
- tick 开放比赛分支 → 纯自然 deadline：`advance_action_opportunity` → `execute_action_resolution`。

### P2 出界涌现（`pass_risk` 调制通道，已按用户二次拍板改为方案 B）
- `sample_pass_landing(from, intended, pass_risk, rng) -> PassLanding { raw, projected, out_side }`（保留）。
- `open_play_pass_risk(pass_m, nearest_def_m, liveness_bonus)`（已实现）+ `open_play_out_probability(risk)`。
- `out_side_for_intended(home, intended, rng)` 定方向；落点沿该轴推过边界（overshoot RNG）。
- `emit_pass_highlight_inner` 出界通道命中 → 出界 pass（NormalPass + goal_line/sideline）。
- 删 `PassOutSource::CornerDirect` 与死球执行链。

### P3 liveness guard
- `ticks_since_meaningful_action` + `LIVENESS_STAGE_{1,2,3}_TICKS`(8/12/16) +
  `liveness_profile(ticks) -> LivenessProfile { forward_intent_bonus, pass_risk_bonus, deadline_pressure_ticks }`。
- 接入四处：deadline / `carrier_move` 前插概率 / `open_play_pass_risk` /
  **`evaluate_open_play_carrier_action` 的无压久持出球档**（用户二次拍板补）。
- meaningful action 重置点：射门 / 成功传球 / 拦截 / 抢断 / 犯规 / 球权变化 / 出界重开 / 松散球拾取。
- 源码区段守卫：liveness 区段内不得出现 `emit_*` / `events.push`（机器检查「只改状态不造事件」）。

### P4 5 分钟统计方向性
- 重写 `p7_frequency_5min_vs_90min_consistent` → `L1_5MIN_SEEDS=1000` cohort 方向性护栏（release/ignore）。
- 90 分钟带保留（离线校准达带）。

### P5 golden v5 + 收尾
- `MODEL_VERSION = 5` + `tests/golden-v5/`（v1-v4 保留）；`gm_legacy_baselines_preserved_and_differs` 扩 v5。
- 重扫 `l2_sent_off_kickoff_seeds` 钉死 seed。
- `./verify.sh` 全绿 + `openspec validate --all --strict`。

## 进展

- [x] P1 删槽位层（代码已删干净，`p31_slot_layer_is_gone` 绿）
- [x] P2 出界涌现（`pass_risk` 调制通道 + 承重守卫）
- [x] P3 liveness guard（四处接入；接入点③时序 bug 已由审阅发现并修复）
- [x] P4 5 分钟统计方向性（测试在 realism target，verify 实跑）
- [x] P5 golden v5 + 收尾（审阅第 1 轮必改项已全部处置）

---

# ✅ 已拍板（2026-09-14 用户二次确认）——三项设计级问题的处置

原「待用户拍板」段的实测证据与根因分析**保留在下方「附：问题证据（归档）」**，处置如下：

## 决定 1：出界改走 `pass_risk` 调制通道（方案 B）

采纳 grill Q2 曾被否的「独立出界 roll」为正确解。承认纯落点误差在当前引擎（意图落点从不贴边）
产不出出界。

- `sample_pass_landing` 纯函数**保留**（负责落点位置；误差足够大时自然越界的分支保留）。
- 新增 `open_play_out_probability(pass_risk)`（出界触发）+ `out_side_for_intended(...)`（方向）。
- 重开映射不变：`out_restart_for(out_side, 最后触球方)`。
- design.md D2 已改写；spec delta 已同步。

## 决定 2：liveness guard 补「无压久持 → 出球」决策档（方案 B）

`evaluate_open_play_carrier_action` 增一档：停滞 ≥ `LIVENESS_STAGE_2_TICKS` 且无压 → 强制选
「出球」候选。guard 仍不直接 emit（只改 `ticks_since_meaningful_action` 状态，carrier 决策因
该状态变化而选出球），事件仍由既有 `emit_pass_highlight_inner` 产。

- design.md D3 已改写为「接入四处」；spec delta 已同步（新增 scenario）。

## 决定 3：接受抢断涌现新频率 + 带重标定

**不接受**「抢断应保持某值」的配额直觉。**不给防守竞争补强制来源**；`shot/tackle` 带按涌现实测
重标定（[1.0,1.8] → [0.5,1.5]，实测 0.835）；同一原则适用于删源后其余频带
（角球 [2,9] → [1.0,7.0]，实测 3.71 → 2.00）。

**⚠️ 实施期更正（审阅发现，2026-09-14）**：拍板时引用的「抢断降至 ~2.6/场」是**中间态**测量
（仅删槽位、未接 liveness 出球档与犯规基线重标定时）。**最终引擎实测抢断 9.40/场**——比 P30 基线
5.60 **上升 68%**。原因：① 删槽位后防守竞争在每个自然 deadline 机会点评估（~994 机会点/场 vs
槽位时代 ~24 槽 + 过渡腿），总评估次数大幅上升；② 犯规基线 0.05 → -0.10（犯规 36 → 23/场）
使部分机会点由犯规改选抢断。design.md D6 已同步更正为实测值。
`foul` 基线改动记录：这是**删源后频带重标定**（犯规原由槽位时代的独立判定产，删槽位后并入
竞争使犯规 36/场溢出 [16,30] 带），非为迁就其他参数。

---

（该节为初次上报时的状态快照；拍板后已继续实现。）

---

## 附：问题证据（归档，2026-09-14 初次上报）

### 删槽位后角球/界外球结构性塌缩（design 级）

**当时触发任务约束 #5**：设计级问题停下标注待确认，未自行调参。以下为该次上报的原始证据与根因，保留备查。

## 症状（删槽位 + 落点误差接线后实测）

| 指标（每场 90min） | P30 基线 | 删槽位后（shift=−4.5） | L1 带 | 状态 |
|---|---|---|---|---|
| 角球发球 `detail=corner` | 3.71 | **0.82** | [2,9] | ❌ 塌缩 |
| 界外球掷球 `detail=throw_in` | 3.96 | **0.03** | [2,9] | ❌ 塌缩 |
| 落点误差致出界（底线/边线侧） | — | **0.0 / 0.0** | >0 | ❌ 恒零 |
| 普通射门 | 6.88 | 6.2 | [6,11] | ✅ |
| 进球 | ~0.95 | 0.95 | — | ✅ |
| 抢断 | 5.60 | **2.6** | shot/tackle ∈[1.0,1.8] | ❌ 比值 2.4 |

`shift` 是「射门推进 hazard 平移」标定参数；−4.5 是把普通射门数落回 [6,11] 带的值。
**注意：角球/界外球塌缩与 shift 无关**——见下面的根因。

## 根因（探针实测，非猜测）

**D2 假设「开放比赛传球的落点会在边界附近、误差可把它推出界」在现有传球模型下不成立。**

探针（200 场 × 90min，n=44635 条有向成功传球，读事件 `x2/y2` = 误差前的**意图落点**）：

```
[落点] 距底线(x) p05=14.7 p25=38.5 p50=41.2 p75=45.6m   <1m:0.000 <2m:0.000 <3m:0.000 <5m:0.000
[落点] 距边线(y) p05=20.3 p25=23.9 p50=25.6 p75=27.0m   <1m:0.000 <2m:0.000 <3m:0.000 <5m:0.000
```

- **没有任何一次传球的意图落点落在距边界 5m 以内**（p05 已是 14.7m / 20.3m）。
- 原因：`lead_point(from, to, lead)` 在传球者与**最近队友**之间插值 60-90%，而队友按
  `formation_target` 站位（离边线/底线都远）。落点因此被结构性地锁在场中央地带。
- 于是要「靠误差出界」，σ 得大到把中圈传球甩出 20-40m —— 那样每一次传球都会变成出界，
  比赛退化为「传球即出界」。**这不是系数标定问题，是机制接不上。**

同样的机制空缺也让**界外球**塌缩：原来界外球 3.96/场里 3.735 来自槽位（`ThrowIn` 抽签），
只剩解围出边线（0.13）顶着 → 0.03。

## 已尝试且不足的方向（记录以免重复）

1. 落点误差按传球距离缩放（σ ∝ 距离 / 30m）：意图落点离边界 20-40m，σ 再大也够不着而不炸。
2. 提高 `pass_risk`（含距离项 + 压迫项 + liveness 加成）：同上，量级差 10-20 倍。

## 可能的解法方向（**待用户拍板，未实施**）

- **A（改传球目标模型）**：给传球落点引入「向边路/底线发展」的战术分量（真实比赛里相当比例
  的传球本来就是往边路/禁区角打的），使意图落点分布自然贴近边界 → 误差才有意义。
  这是**传球选人模型**的改动，超出本 change 声明的范围（design Non-Goals 未提，但等于新增
  一个传球目标选择机制）。
- **B（改出界语义）**：承认「落点误差」不足以产出出界，改回/保留一条**受 pass_risk 调制**的
  出界通道（即 grill Q2 的选项 B「保留独立出界 roll」——当时被 codex 以「两张皮」否决）。
- **C（扩大落点误差的物理含义）**：把误差定义为「传球者瞄准的**边界方向**落点」而非「队友
  位置」——即传球目标本身允许是边线/底线区域（长传解围、边路转移），误差再作用其上。

**第三个待拍板项：liveness guard 三档在真实比赛里拉不回停滞。**
实测（6 场 90min 逐 tick 采样）：`ticks_since_meaningful_action` 全场最大 **3600s**，
远超三档阈值 16s——即存在「连续一小时没有任何 meaningful action」的持球段。
原因：D3 要求护栏**只改行为状态、不直接造事件**，但三个接入点（deadline 缩短 / 前插倾向 /
传球风险）都只能让 carrier **更快地反复做同一个「继续带球」决定**，没有一个能在
「无人逼抢 + 射门推进 hazard 未命中」时强制产出一次传球或射门。即：
**D3 的「非事件型护栏」在「无压持球」这一态下与 D1 的「不得塌缩」互相矛盾**——
要么放宽 D3（允许护栏在某档直接产一次出球），要么给 carrier 决策补一条「无压久持 → 出球」
的几何/停滞条件（仍属行为状态，不改事件生成函数）。

**另需一并拍板：抢断频率 5.60 → 2.6/场**（shot/tackle 比值 2.4 超出 [1.0,1.8] 带）。
原因是抢断此前有槽位 `FallbackSituation::Tackle`（22%×24 ≈ 5.3/场）这条强制评估腿；
删掉后只剩自然 deadline 的防守竞争，而贴身接触在机会点上的联合分布比槽位时代稀疏。
这与角球塌缩同源（都是「删掉的那条腿曾承担事件产出」），但**独立于 D2**，需单独决定：
是接受新频率并重标定 [1.0,1.8] 带，还是给防守竞争补一条涌现来源。

## 当前代码状态（已 commit 的部分）

- P1 删槽位层：完整（`p31_slot_layer_is_gone` 绿，生产代码零残留）。
- P2 落点误差：`sample_pass_landing` / `out_restart_for` / `open_play_pass_risk` 纯函数 +
  单元测试齐备（分类 / 确定性 / 单调 / 重开映射全绿）；**接线已做但产出恒零**。
- P3 liveness guard：`ticks_since_meaningful_action` + 三档 profile + 三处接入已实现；
  `p31_liveness_profile_three_stages_monotone` 等纯函数测试绿。
- 未完成：P4（5 分钟方向性）、P5（golden v5）——依赖上面拍板后的频率落点。
- 临时文件：`engine/tests/p31_probe.rs`（探针，收尾时删）。

---

# 审阅闭环记录

## 第 1 轮（零记忆独立 paseo subagent，对象 `f8198bb`）：**需修改**，5 项必改

| # | 必改项 | 处置（commit） |
|---|---|---|
| 1 | D3 接入点③是死代码：`note_meaningful_action` 早于 emit 归零 → `pass_risk_bonus` 恒读 0（实测 73890 采样 0 次非零）；违约 spec「三层递进 SHALL 调传球风险」 | `96c1a79` 归零移到 emit 之后 + 执行层绑定 + 回归守卫（变异验证） |
| 2 | D6 文档方向写反：写「抢断降至 2.6/场」，实际 9.40/场（升 68%），主因是本 change 的 liveness 第四档 | `e35fe47` 按实测 + 变异定位改写 |
| 3 | D4 守卫在 CI 里从不执行（lib 的 `#[ignore]` 不被 `--test realism -- --ignored` 扫到）；两条空转断言 | `96c1a79` 挪进 realism（verify 由 7 → 8 个 L1）+ 换真断言 |
| 4 | delta spec 非 MODIFIED 全量复制 → 归档会静默删主 spec 的 P29/P30 内容（scenario 61→57）；新 requirement 错放导致 archive abort | `e6607e8` 全量复制 + ADDED 段拆分 + 频带 requirement 同步 |
| 5 | 出界通道「pass_risk 调制」无生产级断言（`GAIN=0` 时全绿 = D2 声称已抛弃的「两张皮」） | `96c1a79` 绑定最终概率 min/max + 跨度断言（变异验证） |
| 6-10 | 次要：jockey 结构性不可达被 `\|\|` 掩盖、只写不读计数、无事件守卫范围不足、标定注释过期、死分支 | 分别在 `e35fe47`/`02800cb`/`a417e5f`/`96c1a79` 清理 |

**审阅确认的机制正面结论**（可查证）：删槽位删得干净（含隐性配额检查）；出界通道是新承重机制
（变异：通道置 0 → 出界 16.77 → 0.20/场）；删 `CarrierAction::Dribble` 确有必要（否则 contain
结算归零 → P30 D5 压力状态机制死）；golden v5 规范（v1-v4 未覆盖、逐 seed 不同、守卫能抓空改动）；
各频带重标定有实测对照（`BASE_DEF_FOUL=0.05` 复原 → 犯规 36.55 超带，证其承重）。

## 第 2 轮（对象 `96c1a79`）：进行中

审阅重点 = 修复本身是否引入新问题（归零时序的副作用、新绑定字段是否只写不读、
`OUT_CHANNEL_RISK_GAIN` 0.11→0.062 的依据是否记录、5min 测试挪 target 后的耗时）。

**自查补充**：`OUT_CHANNEL_RISK_GAIN` 的重标定依据已补注释（`0.11` 是接入点③未生效时的标定；
修复后 `pass_risk` 带上 liveness 分量 → 出界偏多 → 下调至 `0.062` 回带）。这是「通道生效后的
重标定」，非为迁就其他参数。
