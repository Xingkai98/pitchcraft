# P17 进度：GitHub Actions CI（verify.sh 自动跑）

分支：`p17-github-actions-ci/2026-09-10`（worktree）
最终实质 commit：`2b99278`（其后提交均为进度/文档类，不含 CI 配置变更）

## 交付物

**唯一改动文件：`.github/workflows/ci.yml`**（新）。纯基础设施，不碰引擎 / viewer / 协议 / 测试代码，
`verify.sh` 与 `package.json` 均未改。

## yaml 内容要点

单 job（`verify`），`runs-on: ubuntu-latest`，`timeout-minutes: 30`。步骤顺序严格满足 `verify.sh`
的前置依赖链：

| 顺序 | 步骤 | 要点 |
|------|------|------|
| 1 | `actions/checkout@v7` | v4 的 action 运行时（Node 20）已被 GitHub 弃用，取当前 major（Node 24） |
| 2 | `dtolnay/rust-toolchain@stable` + `targets: wasm32-unknown-unknown` | verify.sh 第 1 步 `cargo test` 与后续 wasm 编译都靠它 |
| 3 | `Swatinem/rust-cache@v2`，`workspaces: engine` | 缓存 `engine/target`（wasm release 产物与 cargo test 共用） |
| 4 | `cargo build --target wasm32-unknown-unknown --release` → `cp … ../viewer/engine.wasm` | **关键前置**：`viewer/engine.wasm` 被 gitignore，CI 必须自编，否则 verify.sh 第 3 步 exit 1 |
| 5 | `actions/setup-node@v7`，`node-version: '22'`，`cache: npm` | app.test.js 的 loader 用 `module.registerHooks`（需 Node ≥ 22.15） |
| 6 | `npm ci` | 有 `package-lock.json`，确定性安装 |
| 7 | `./verify.sh` | 四步全套 |

触发：`on: { push:, pull_request: }` —— feature 分支 push 即触发（迭代 CI 不用先开 PR），PR 合入前也跑。

并发：`concurrency: { group: <workflow>-<ref>, cancel-in-progress: true }`（审阅建议采纳）——
同 ref 连续 push 取消未跑完的旧 run，避免并行占 runner。

## CI 运行结果（是否真触发 / 真跑绿）

**Actions 已启用**（`gh api repos/Xingkai98/pitchcraft/actions/permissions` → `enabled: true`），
三次 push **全部真实触发**并**全部 success**。非「声称绿」——每次均从 job 日志逐项核对过四层验证。

| run | commit | 事件 | 结果 | 耗时 | 说明 |
|-----|--------|------|------|------|------|
| [34465412179](https://github.com/Xingkai98/pitchcraft/actions/runs/34465412179) | `ce5fe11` | push | ✅ success | 1m47s | 首跑即绿 |
| [34465656163](https://github.com/Xingkai98/pitchcraft/actions/runs/34465656163) | `d2cb92c` | push | ✅ success | 1m20s | action 升 v7 后复跑绿 |
| [34467394033](https://github.com/Xingkai98/pitchcraft/actions/runs/34467394033) | `2b99278` | push | ✅ success | 1m2s | 加 concurrency 后复跑绿 |

远端 job 日志实证（`gh api repos/Xingkai98/pitchcraft/actions/jobs/<id>/logs`）：

```
=== 1/4 Rust 引擎测试（cargo test）===
=== 2/4 Viewer 单测（node --test，含 app.test.js DOM 测试）===
# tests 251
# pass 251
# fail 0
=== 3/4 WASM 端到端（v2 并行节拍：engine.wasm → viewer 播放无 snap）===
E2E v2 OK：5817 事件 | … | 无顶层 dribble | config 生效 | 无 snap(4 seed: …)
=== 4/4 真实性统计套件 L1（realism 统计分布，release 显式跑；…）===
=== 全部验证通过 ===
```

### 迭代记录（首跑前已预计，实际只发生 1 次真实修复）

1. **首跑（ce5fe11）即绿**——yaml 一次写对，无路径 / 版本 / 超时类失败。
2. **唯一修复（d2cb92c）**：首跑绿但带 GitHub 弃用告警——`checkout@v4` / `setup-node@v4` 的
   action 运行时为 Node 20，已被强制跑在 Node 24 上。升到 `@v7`（Node 24 系列），触发条件与用法不变，
   `node-version` 仍为 22。此后 run 无任何 annotation、无弃用告警。

## Review 结论（2 轮）

审阅用**独立 paseo agent**（`codex/gpt-5.6-sol`，full-access，agent-scoped 落本 workspace，
零记忆、只读）。选异族模型（非 Claude）以取真正独立的视角。

### Round 1（审 `d2cb92c`）：**通过，无遗留问题**

审阅 agent 独立核验（非复述）：`gh run list` 确认 run `34465656163` 为 push 触发、`success`；
从 job 日志逐项确认 251/251、`E2E v2 OK`、realism `7 passed`、`全部验证通过`，无 skip；
并自行核实 `rust-cache` 的 `workspaces: engine` 语义——日志中
`Cache Paths: …/engine/target` 证实缓存路径正确。

提出 2 条建议（均为「建议」级，非阻塞）：

| # | 建议 | 处理 |
|---|------|------|
| 1 | 无 `concurrency`，快速连续 push 会并行跑多个 CI | **采纳**（commit `2b99278`） |
| 2 | action 用 major tag 而非 commit SHA pin | **不采纳**：design D2/D3 明确用惯例 major tag，首方/知名 action 用 major tag 是通行做法 |

### Round 2（审 `2b99278`，即 HEAD）：**第二轮审阅通过，无遗留问题**

审阅 agent 逐条核验 concurrency 修复：
- `group: ${{ github.workflow }}-${{ github.ref }}` + `cancel-in-progress: true` 位置（`on:` 与 `jobs:`
  之间）、表达式、布尔值均正确，与 GitHub 官方文档示例一致；
- **证伪自取消风险**：新 run 进入同 group 只取消旧的 pending/in-progress run，不自取消；
  run `34467394033` 实际跑完 `success` 直接反证；
- `github.ref` 对 push（`refs/heads/…`）与 PR（`refs/pull/<n>/merge`）均合理，不同分支/PR 互不取消；
- 未破坏原有触发与步骤（`git show HEAD` 只新增 6 行）。

并独立确认新 run `34467394033`（HEAD `2b99278`，push）为 `success`，四层验证完整执行，无 skip。
工作树保持干净，未改任何文件。

## 本地验证

- `./verify.sh` → **4/4 步全通过**（Rust cargo test / viewer 251 pass / WASM e2e `E2E v2 OK` / realism L1 7 passed）
- `actionlint 1.7.7 .github/workflows/ci.yml` → 退出码 0，无告警
- `openspec validate --all --strict` → **17 passed / 0 failed**（含 `change/p17-github-actions-ci`）

## 最终 commit

| commit | 内容 |
|--------|------|
| `ce5fe11` | P17 实施：GitHub Actions CI（verify.sh 自动跑） |
| `d2cb92c` | P17 修复：CI action 升到 Node24 运行时（checkout@v7 / setup-node@v7） |
| `2b99278` | P17 审阅修复 r1：CI 加 concurrency 取消同 ref 旧 run |

以上三个提交构成本 change 的**全部实质改动**（`2b99278` 为最后一次改动 CI 配置的提交）。
此后仅有进度/文档类提交（如本 `.p17-progress.md` 及其回填），不影响 CI 配置与结论。

基线：`99a72cc`（P17 立项）。`2b99278` 与紧随其后的归档提交均已 push 且 CI ✅ success
（run [34467394033](https://github.com/Xingkai98/pitchcraft/actions/runs/34467394033)、
[34467981162](https://github.com/Xingkai98/pitchcraft/actions/runs/34467981162)）。

### 未决 / 说明

- **无 open question**（design 已定，本 change 全部按 design 落地）。
- **不涉及** `viewer/index.html` 的 `?v=` 版本号：本 change 未改 `viewer/` 下任何 JS/HTML，
  按 CLAUDE.md #4 无需 bump。
- 仓库 Actions 已启用，CI 配额/权限无问题——push 即触发，无 pending/无 runner 情况。
- 后续若要合入 PR，PR 触发的 CI 与本分支 push 的 CI 因 ref 不同会各跑一次，属「双触发」设计的
  正常结果，非缺陷（Round 2 审阅已确认）。
