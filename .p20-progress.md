# P20 进度归档：观察诊断前的「事件锚定确认」步

- Change：`openspec/changes/p20-observation-confirmation/`
- 分支：`p20-observation-confirmation/2026-09-10`
- 立项 commit：`32120bc`（= main 上 `5d7991b` 的重复提交，见下方「备注」）
- 实施 commit：`6002ea4`
- 审阅修复：`82d5d4b`(r1) → `b081459`(r2) → `3dd345d`(r3) → `3d1a231`(r4)
- 用户拍板 spec 收敛：`cf8ff78`
- 最终 commit（HEAD）：`3d1a231`

## 目标

在「采集 → captured」与「诊断」之间插入事件锚定确认步：

```
captured → [提案] → awaiting_confirmation → [确认] → confirmed → [诊断] → 终态
```

确认的唯一产物 = 事件 index 集合；诊断以确认的 index 集合为锚点。

## 改动文件

### 新增

| 文件 | 内容 |
|------|------|
| `viewer/event-labels.js` | 人话标签共享模块 `describeEvent(e, lineup)` + `isCandidateEvent(e)`。双端共享（viewer 页面 + tools CLI）。 |
| `viewer/event-labels.test.js` | 10 例：类型分派、detail 区分（replace/suffix 两类）、beat 折叠、lineup 队别、兜底。 |

### 修改

| 文件 | 内容 |
|------|------|
| `tools/runner.mjs` | `TASK_STATUSES` 加 `awaiting_confirmation`/`confirmed`；`runProposal`（提案模式）、`buildProposalPrompt`、`normalizeProposal`、`anchorEventsBlock`、`confirmTask`（确认校验+流转，service/CLI 共用）、`CONFIRM_SOURCES`/`CONFIRMABLE_STATUSES`；`buildDiagnosisPrompt` 加 `anchors` 形参；`runDiagnosis` 保留旧任务的 proposal/confirmation 并把锚点喂给诊断 prompt。 |
| `tools/runner-cli.mjs` | 加 `--propose`（只提案不诊断，只需 `--bundle`+`--tasks-dir`）。 |
| `tools/service.mjs` | `POST /tasks/:id/confirm`；`GET /tasks/:id` 返回 `proposal`/`confirmation`/`events`/`lineup`；`import confirmTask, redactTaskText`。 |
| `tools/queue-cli.mjs` | `propose`/`events`/`confirm` 子命令；`buildProposeArgs`、`isProposable`、`isActionable`、`statusHint`/`listHint`/`nextStepHint`、`parseEventIndexes`、`formatEventLines`；`isRerunnable` 收 `confirmed`。 |
| `viewer/app.js` | `awaiting_confirmation` 态 A+B 提案 UI（`renderConfirmationInto`）+ `submitConfirmation`；`confirmed` 态「已确认等待诊断」；轮询回填 `confirmationDetail`；确认数据缺失时的「加载中/错误+CLI 回退」分支；版本号 bump。 |
| `viewer/dom-test-harness.mjs` | 加 `clickSelector`（按 CSS 选择器点无唯一 id 的按钮）。 |
| `verify.sh` | 加第 3/5 步跑 `tools/*.test.mjs`（补既有缺口）；步骤号 4→5 重排。 |
| `.github/workflows/ci.yml` | 任务名 + 步骤号注释随 verify.sh 5 步更新。 |
| `viewer/e2e-v2.mjs`、`engine/tests/realism.rs`、`README.md` | 同步 verify.sh 步骤号（3→4、4→5）。 |
| `viewer/observation-list.js` | `OBSERVATION_STATUSES` 加两态；P20 纯 helper：`confirmationDetailFromTask`、`proposalIndexes`、`defaultConfirmationSelection`、`toggleEventIndex`、`confirmationEventsToShow`。 |
| `viewer/index.html` | 两态徽章 CSS + 确认面板 CSS；`app.js?v=20260912-2`。 |
| `.scratch/issues/12-observation-confirmation.md` | 设计定稿（从 main checkout 拷入 worktree，此前未跟踪）。 |
| `openspec/changes/p20-observation-confirmation/{design.md,proposal.md,specs/match-observation/spec.md}` | 确认步强制范围收敛为 queue-only（用户拍板 finding 3）。 |
| `openspec/changes/p20-observation-confirmation/tasks.md` | 全部勾选完成。 |

### 测试文件

`tools/runner.test.mjs`（+11 例）、`tools/service.test.mjs`（+8 例）、`tools/queue-cli.test.mjs`（+13 例，含真实 CLI 子进程端到端）、`viewer/app.test.js`（+9 例，jsdom harness）、`viewer/observation-list.test.js`（+6 例）、`viewer/event-labels.test.js`（新，10 例）。

## 关键实现决策（design 未明确覆盖的分叉）

### 1. 人话标签共享模块落在 `viewer/`（而非 `tools/`）

- **硬约束**：`viewer/index.html` 用 `<script type="module" src="./app.js">`，页面由 `viewer/serve.py` 在 **viewer/ 目录**起静态服务。浏览器里 app.js 只能 import viewer/ 内的模块，`../tools/xxx.mjs` 会 404。
- 反向引用 tools→viewer 有先例（`tools/github.mjs` import `../viewer/audit-report.js`）。故 `viewer/event-labels.js` 由 viewer app.js 与 `tools/queue-cli.mjs`/`tools/runner.mjs` 双端 import。
- 注：任务提示里的「tools/bundle.mjs 已被 viewer 引用」实际是 `viewer/*.test.js`（测试）引用，不是浏览器运行时，不能照搬。

### 2. `GET /tasks/:id` 额外返回窗口 `events` + `lineup`

- 页面 B 面（「从全部事件重选」）要用人话标签渲染事件，而刷新后页面只有 localStorage 元数据、拿不到 bundle。design D7 只写了「返回 proposal + confirmation」。
- 让页面直接调 `describeEvent(e, lineup)` 渲染，才真正满足「双端共用同一份标签函数」（若改由服务端预生成标签串，页面就不共用该函数了）。都是观察者自己窗口内的数据，`statement` 已是同类先例。

### 3. `confirmTask` 抽到 runner.mjs 供 service 与 queue-cli 共用

- 避免两处确认校验/状态流转漂移。service 只负责 HTTP 映射（400/404/409）+ 落盘；queue-cli 直接调同一函数。

### 4. detail 标签分「replace / suffix」两类

- `corner`/`throw_in`/`free_kick` 是完整事件名，**顶替** type（角球其实是 pass，但用户必须一眼看出是角球而非普通传球）；`out_sideline`/`out_goal_line`/`header`/`foul_trip` 是 type 的限定，拼在 type 后（传球出边线 / 射门头球）。

### 5. 页面文档级 `describeEvent` 命名冲突

- app.js 原本已有调试用 `describeEvent(e, id)`（供 `#event-info` 面板）。共享模块的 `describeEvent` import 时别名为 `describeWindowEvent`，避免遮蔽。

## 测试结果

```
Rust 引擎     cargo test                 —— verify.sh 1/5 通过
Viewer 单测   node --test *.test.js      —— 288 / 288 通过（含 app.test.js DOM 用例）
Tools 单测    node --test tools/*.test.mjs —— 321 / 321 通过
WASM e2e      node e2e-v2.mjs            —— E2E v2 OK（5817 事件，无 snap）
realism L1    cargo test --test realism  —— 7 passed
verify.sh     ./verify.sh                —— 五步「全部验证通过」
openspec      openspec validate --all --strict —— 17 passed, 0 failed
```

> 注：本 change 顺带补上既有缺口——`verify.sh` 原先**只跑 viewer 单测**，`tools/*.test.mjs`
> 从未被任何脚本或 CI 执行。现已加为 verify.sh 第 3/5 步（CI 的 `./verify.sh` 随之覆盖），
> 并修正相关步骤号注释（e2e-v2.mjs / realism.rs / README.md / ci.yml）。

手工 smoke（真实进程，非单测）：
- CLI：`propose`（无 key → fallback-empty，状态 awaiting_confirmation）→ `events`（渲染 `#55 · t=51s · 传球出边线 · 主队 #7`）→ `confirm --events 55`（写 confirmed + 锚点）全部正确。
- HTTP 服务：`GET /tasks/:id` 返回 proposal/events/lineup；`POST /tasks/:id/confirm` 200 写确认；非法 event_indexes → 400；未知任务 → 404。

## 代码审阅闭环

### 审阅 r1（paseo agent `75752c24`，零记忆独立审阅）

- 审阅对象：`git show 6002ea4`；方法：读全部 spec + diff、跑三套测试 + verify.sh + openspec
  validate、真实 CLI 端到端、6 处「删行变红」变异测试验证测试真驱动接线。
- 结论：**FIX-FIRST**，9 项发现（4 MAJOR / 3 MINOR / 2 NIT）。测试真实性判定 PASS。
- 修复 commit `82d5d4b`（详见该 commit message）：
  | # | 级别 | 问题 | 处置 |
  |---|------|------|------|
  | 1 | MAJOR | `queue-cli list` 丢弃 awaiting_confirmation 任务，且新态被误标「失败，可重试」 | 加 `isActionable`/`statusHint`/`nextStepHint`，按状态给准确提示 |
  | 2 | MAJOR | re-propose 已 confirmed 任务会把状态退成 awaiting_confirmation → 任务卡死（run 不收、也不等人确认） | 有 confirmation 时保持 confirmed |
  | 4 | MAJOR | `tools/*.test.mjs` 从未被任何脚本/CI 执行（P20 主要新增覆盖都在这里） | `verify.sh` 加第 3 步；端到端测试改绝对路径不依赖 cwd |
  | 5 | MINOR | 兼容测试 fixture 不含 events（不真实）；null 守卫是死代码 | 改用真实旧任务 payload + 注释说明真正兜底的是分支顺序 |
  | 6 | MINOR | `queue-cli propose` 静默丢弃 `--model` 等 provider 覆盖参数 | 转发同一套 argMap |
  | 7 | MINOR | 确认按钮 id 在多条目时重复（无效 HTML） | 改 class + harness 加 `clickSelector` |
  | 8 | NIT | `runProposal` 重试循环每分支都 break（while 是装饰） | 改单次尝试并注明 |
  | 9 | NIT | 未使用的 `CONFIRMABLE_STATUSES` import | 删除 |
  | 3 | MAJOR | **未自行修改**：spec「未确认前诊断 SHALL 不启动（非 queue-only 服务亦同）」与非 queue-only 服务「提交即自动诊断」的实现冲突 | 见下方「待确认问题」 |

### 审阅 r2（paseo agent `5312d3f2`，零记忆独立审阅）

- 审阅对象：`git show b081459`（复核 r1 九项修复）+ 新问题扫描；方法同上 + 变异测试。
- 结论：r1 九项**全部 FIXED**（finding 3 除外，仍未改，属已升级的开放项）；另提 8 项新发现
  （N1–N8，无 MAJOR/BLOCKER）。修复 commit `b081459`：
  | # | 级别 | 问题 | 处置 |
  |---|------|------|------|
  | N1 | MINOR | runProposal 对首态非 captured 的任务再提案会写空 status_history | 非空历史整体带上 |
  | N2 | MINOR | propose 转发 provider 参数的修复无测试（删掉转发循环测试仍全绿） | 抽 `buildProposeArgs` 纯函数 + 单测 |
  | N3 | MINOR | `list` 的可取过滤无测试（换回 isRerunnable 测试仍全绿） | 加 list 端到端测试 |
  | N4 | MINOR | 确认数据未就绪时按钮可点 → POST 空锚点，可能覆盖 CLI/对话面已确认的锚点 | 未就绪时禁用 + 「加载确认数据…」提示 |
  | N5 | NIT | app.js 改动后版本号未 bump | 20260912-1 → 20260912-2（index.html + import + VIEWER_SOURCE_REVISION） |
  | N6 | NIT | CI 注释/任务名仍是「四步」 | 改 5 步标签 + 任务名加 tools 单测 |
  | N7 | NIT | listHint/nextStepHint 只看状态，非入队源的失败任务误标「可重试」（run 实际拒收） | 按 `isRerunnable(task)` 判 |
  | N8 | NIT | confirmed 卡片在下一轮轮询前短暂显示「未锚定具体事件」 | POST 返回的 confirmation 折进条目 |
- N4/N8/N3 三处修复均做了删行变红验证（测试真驱动接线）。

### 审阅 r3（paseo agent `ba596412`，零记忆独立审阅）

- 审阅对象：`git show 3dd345d`（复核 r2 的 N1–N8）+ 新问题扫描；6 处变异测试。
- 结论：**SHIP**。N1–N8 全部真实修复，4 处「必翻红」变异各命中对应用例。另提 3 项
  （NEW-1 MINOR + N6 残留 + NEW-2 NIT，外加 N2 调用点守卫），修复 commit `3dd345d`：
  | # | 级别 | 问题 | 处置 |
  |---|------|------|------|
  | NEW-1 | MINOR | r2 的 N4 修复引入的回归：proposal 因 bundle 不可读失败时，任务停在 awaiting_confirmation 但服务不再返回 events（`events: []`）→ `confirmationDetail` 永远为 null → 卡片渲染一个永远禁用的死按钮 | confirmationDetail 为空时不进确认面板，改走「拿不到窗口事件数据 + CLI 回退」；删掉 renderConfirmationInto 里已不可达的 !ready 守卫 |
  | N6 残留 | NIT | e2e-v2.mjs / realism.rs / README.md 的 verify.sh 步骤号随 4→5 过期 | 3→4、4→5 更新 |
  | NEW-2 | NIT | statusHint 未知状态谎报「可重试」 | 改「(未知状态)」 |
  | N2 残留 | NIT | buildProposeArgs 只测了函数内部，删调用点 opts 仍全绿 | 补 propose 端到端守卫（--model/--permission 落进任务文件） |
- NEW-1 与 N2 调用点的测试均删行翻红验证。

### 审阅 r4（paseo agent `4b7cd362`，零记忆独立审阅）

- 审阅对象：`git show 3dd345d`（复核 r3 四项修复）+ 回归检查。结论：**SHIP**，四项全部
  FIXED，4 处变异各命中对应用例；NEW-1 修复后正常的确认卡片路径仍被有效守卫（反转变异令
  6 个用例翻红）。
- 另提 1 处外观 NIT（非阻塞）：刷新页面时确认数据是内存派生态，awaiting_confirmation
  卡片在首轮轮询前会显示「拿不到窗口事件数据…提案可能失败」的失败文案（实际只是加载中）。
  已在 `3d1a231` 处理：有 `detail_error` 才报错 + 给 CLI 回退；否则显示中性「正在加载确认数据…」。

### Finding 3 —— spec 与非 queue-only 服务行为的冲突（**已解决：用户拍板选选项 1**）

- 原冲突：spec `specs/match-observation/spec.md:7`「未确认前诊断 SHALL 不启动（**非 queue-only 服务亦同**）」与
  `design.md:31`（D1）「非 queue-only 可自动跑」自相矛盾；实现跟随后者（`service.mjs` 的 `if (queueOnly)` 之外提交即诊断）。
- **处置（用户拍板，选项 1「改 spec 措辞」，commit `cf8ff78`）**：三处口径统一为——
  确认步在 **queue-only 模式强制**（未确认前诊断不启动），非 queue-only 模式为**可选增强**
  （诊断随提交自动启动，确认步仍可用于给诊断提供锚点）。
  - `specs/match-observation/spec.md` 需求句改写；
  - `design.md` D1 的 confirmed 语义 + 新增「确认步的强制范围」一条；
  - `proposal.md` Why/What 段落同口径。
- 验证：`openspec validate --all --strict` → 17 passed, 0 failed。

## 最终结论

- **无遗留问题**：审阅 r1（FIX-FIRST，9 项）→ r2（r1 全修复，N1–N8）→ r3（SHIP，NEW-1/N6/NEW-2/N2）
  → r4（SHIP，1 外观 NIT）→ 全部处理。唯一升级给用户的 Finding 3 已由用户拍板解决。
- **测试**：viewer 288 绿 + tools 321 绿 + `cargo test` 绿 + WASM e2e 绿 + realism L1 绿；
  `./verify.sh` 五步「全部验证通过」；`openspec validate --all --strict` 17/17。
- 改 viewer JS 已按项目 CLAUDE.md §4 bump `index.html` + app.js 顶层 import 版本号至 `20260912-2`。

### 待确认问题

（无。Finding 3 已解决。）

## 备注：重复立项 commit（需主 session 决定合入方式）

worktree 分支上 P20 立项被**重复提交**了一次：

- `32120bc`（本 worktree 分支上）：内容与 main 上的 `5d7991b` 完全相同，仅 hash/时间戳不同，父链相同（都基于 `2ee7b96`）。
- 成因：主 session 在 main 上提交立项后建了 worktree；本 agent 又在 worktree 里 `git add + commit` 了一遍同样的立项文件。

**处理**：未改写历史、未 force push（遵守约束）。实施 commit `6002ea4` 基于 `32120bc`。合入 PR 时二选一，由主 session 决定：
1. **squash merge**：立项重复在 squash 后自然消失，历史干净；
2. **把分支 rebase 到 main `5d7991b`**：丢弃 `32120bc` 这个重复 commit（`git rebase --onto 5d7991b 32120bc` 或交互式 drop），再开 PR。

本 agent 不擅自 rebase（会影响进行中的实现）。等待主 session 指示。

## 其它发现

- `openspec` CLI 在 `~/.npm-global/bin`，不在默认 PATH。

## 待办

- [ ] 代码审阅闭环（review agent 运行中）。
- [ ] 合入方式由主 session 定（见「备注」）。
