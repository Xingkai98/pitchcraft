# P27 进度：出界事件协议迁移（#25 阶段 1）

分支 `p27-out-of-play-protocol/2026-09-13`｜工作目录 worktrees/1g1x3st4/p27-out-of-play-protocol-2026-09-13

## 0. 现状勘察（已完成）

出界 pass 的三条生产路径（全部 `result:"contested"` + `detail:"out_*"` + 落点 clamp01 回边界）：

| 路径 | 位置 | detail | 重开 |
|---|---|---|---|
| 槽位出界（corner/throw_in 槽） | `emit_pass_out_play_slot` lib.rs:1289 | out_goal_line / out_sideline | source=CornerDirect→角球 / NormalPass→界外球 |
| 普通传球出界（P6/P7 allow_out 分支） | `emit_pass_highlight_inner` lib.rs:1361 | out_goal_line / out_sideline | NormalPass→门球/界外球 |
| 头球解围出界（battle 防方胜 90/10 档） | `emit_clearance` lib.rs:2360/2367 | out_goal_line / out_sideline | Clearance→角球/界外球 |

保持 `contested` 的三条真·争抢落点：`start_goal_kick`(lib.rs:1917)、`emit_corner_kick`(2036)、以及槽位角球/界外球**发球**（`emit_corner_kick`/`emit_throw_in` 的 `success`）。

关键几何：`HighlightOutcome::PassOutOfPlay.out_pos` 现存 clamp 后投影 → `highlight_ball_end` 用它当飞行期 `st.ball_pos`（→ `formation_target`，无 RNG）→ `finalize_highlight` 用它做 `st.ball_pos` 与重开锚（`corner_flag` / `throw_in_spot` 内部再 clamp）。

## 1. 实现计划

### P1 引擎（lib.rs）
- P1.1 `Event` 加 `out_pos: Option<(f64,f64)>`（Default None）+ `to_json` 输出 `out_side`/`out_pos`（位置：detail 之后）；`out_side: Option<String>` 字段。
- P1.2/1.3/1.4 三处出界路径：`result="out"`、加 `out_side`（detail=out_goal_line→goal_line / out_sideline→sideline）、`out_pos=Some(raw)`、`x2/y2` 保留 clamp 投影、`detail` **保留**（viewer/game.js/event-labels.js/realism 计数都读它）。
- D3：`HighlightOutcome::PassOutOfPlay.out_pos` 存 raw；飞行时间用 raw 终点。

### P2 viewer
- P2.1 `protocol.js`：pass detail 枚举不变（detail 保留）；**补** `out_side` 枚举校验（goal_line/sideline）+ `out_pos` 不做 [0,1] 范围校验（可越界，只校验有限数）+ `result` 枚举不校验（现状即不校验）。index.html 版本号 +1。

### P3 detector
- P3.1 `outEvidenceOf` 顺序：`result==='out'` → `out_side` 存在 → `detail` out_* → 几何。
- P3.2 `classifyPassOutcome` 复用同一函数（自动同步）；`outReasonOf` 补 out_side 分支。
- P3.3 `detector-field-contract.mjs`：unforced_out / pass_outcomes 的 reads 补 `out_side`（producer=engine），notes 更新。

### P4 golden 版本分离
- P4.1 `MatchConfig` 加 `model_version: u32`（默认 1；`MatchConfig::default_()` → v2？→ 见下）
- P4.2 `engine/tests/golden-v2/seed-*.json` 重基线；v1 保留不覆盖。

### P5 测试
- P5.1 出界 pass 字段断言（result=out / out_side / x2y2∈[0,1] / out_pos 越界）
- P5.2 **D6 逐 seed 对比 v1**：v1 golden 的 n_events/类型分布/比分 == v2 现跑值；stream_hash 仅在有出界 pass 的 seed 变。
- P5.3 verify.sh + openspec validate
- P5.4 独立审阅闭环

## 2. R1 实测：D3 与 D6 冲突（已用探针证实，取其解）

**先看 design 内部矛盾**：D6 写「验收必须证明：事件数量、**事件时序**、比分逐 seed 与 v1 完全一致」，
而 D3 写「飞行时间/方向计算用真实终点」。飞行时间 `flight` 直接决定 `t_end`（=事件时序）与
`ceil(flight)`（=飞行期 beat 数量），两者数学上不可兼得。用户把 D6 标为「硬验收，做错了要重来」，
故 D6 优先。

**结论：D3 的「飞行时间/方向用真实终点」+「内部 out_pos 存 raw 并回灌模拟」与 D6 的硬验收（事件数量/时序/比分逐 seed 与 v1 完全一致）数学上不可兼得。实测数据：**

方法：把 change 前（`git stash` 后重建）与各探针变体的 WASM 各跑一遍，逐事件逐字段比对（脚本 `.scratch/notes/d6-compare.mjs`，seed 1-10 × 90min，约 5.8 万事件）：

| 变体 | 改动 | 事件数不一致 seed | 非预期字段差异 | 结论 |
|---|---|---|---|---|
| post-probe | raw 终点算 flight **且** out_pos 存 raw（D3 字面） | 5/10 | 50010 | D6 FAIL |
| variantA | 只改 flight 用 raw 终点 | 3/10 | 23454 | D6 FAIL |
| variantB | 只改 out_pos 存 raw（flight 不动） | 5/10 | 49363 | D6 FAIL |
| **variantC** | **out_pos 存 raw，但在两个回灌点 `clamp01`** | **0/10** | **0** | **D6 OK** |

**根因（探针实测，非推测）**：出界几何有两条回灌模拟的路径，且引擎对初值敏感（混沌）：
1. `t_end = t + flight` — 高亮结束 tick 决定后续所有事件落点。raw 终点让距离 +0.7~5 m → flight +0.03~0.4 s → `ceil(flight)` 翻档 → 高亮少/多产 1 个 beat → 事件数变。
2. `highlight_ball_end` → 飞行期 `st.ball_pos = out_pos`(raw，可越界) → `formation_target` 的 `ball.0/ball.1` 项 → 22 人跑位目标偏 ~1e-3 → mover 坐标漂移 → `nearest_defender` 距离变 → 拦截/传失分支翻转 → 全流级联。

**采用解（variantC）**：`HighlightOutcome::PassOutOfPlay.out_pos` 仍存**真实越界值**（D3 的存储语义保留，阶段 2/3 可直接取用），但在**两个回灌模拟的消费点显式 clamp**（`highlight_ball_end`、`finalize_highlight` 的 `st.ball_pos`/重开锚），使模拟轨迹逐字节与 v1 一致。事件字段 `x2/y2` 用投影点、`out_pos` 用 raw —— 协议面完整达成 D2。

**唯一偏离 D3 的点**：D3「飞行时间/方向用真实终点」未采用 —— 因为 flight 是回灌路径 (1)，改它必然破 D6。任务书把 D6 标为「硬验收，做错了要重来」，故 D6 优先，此处显式记录偏离。

**遗留（阶段 2/3 处理）**：`out_pos` 存 raw 但要靠调用点记得 clamp —— 阶段 2/3 若要让出界几何真正回灌（涌现出界），需移除这两处 clamp 并接受流哈希变化（届时本就该重基线）。

## 3. 执行记录

- [x] 探针实验（见 §2）
- [x] P1 引擎出界事件字段（commit 9dae473）
- [x] P2 viewer 协议 + 标签（305da49）
- [x] P3 detector 出界证据优先 result=out（9bbc07d）
- [x] P4 golden 版本分离（f5e9b40）
- [x] P5 字段断言 + D6 工具 + 门槛（b208a8b）
- [x] 审阅第一轮（claude/fable-5 + codex/gpt-5.6-sol 并行，零记忆）→ 修复（03ade98）
- [x] 审阅第二轮（复核修复）→ 修复（2ba6ba3）
- [x] 审阅第三轮（确认修复无遗留）→ 修复（ea07bab + P3 注释）

### 第二轮审阅结论
- codex：**无 P0、无 P1**；仅 1 个 P2（tasks.md 复选框未打勾）。
- claude/fable-5：**无 P0**；发现两轮都漏的 **P1——spec delta 无法归档**：
  `### Requirement: pass 结果枚举扩展` 在主 spec 中不存在（只是 `pass 拦截字段（interceptor）`
  下的一个 Scenario），`openspec validate` 通过但 `openspec archive` 会 abort
  （"MODIFIED failed for header - not found"）。**我已独立复现**（/tmp 副本跑 archive）并修复：
  改为 MODIFIED 实际存在的 `pass 拦截字段（interceptor）`，五值枚举并入该条；实测 archive 成功。
  教训：**`validate` 不校验 MODIFIED 需求名是否存在 → validate 全绿 ≠ change 可归档。**
- 另修 2 处 P2/P3 陈旧文本（dead_ball 死分支理由、detectors.test 出界形状注释）。

### 第一轮审阅结论
- claude/fable-5：**无 P0**；D6/D1/D2/D4/D5 五项硬验收全部独立复现通过（自建 pre 树、
  自写「去字段后逐字节全等」更强探针、移 clamp 重建反证哪处承重）。发现 P1/P2/P3 各若干。
- codex/gpt-5.6-sol：**1 个 P0**（`model_version` 与 D5 文本不符 + 空转字段，
  实测 `{model_version:1}` 与 `{model_version:2}` 输出逐字节相同）；其余验收全部独立通过。
- 修复：documented D3 偏离同步进 design.md（P1）；model_version 明确为「仅选 golden 目录」
  并改文本/注释（P0）；fixture provenance 文案（P2）；derive 层 out_pos 米制化（P2）；
  outEvidenceOf 枚举对齐（P3）。见 commit 03ade98。

## 3b. 门槛实测证据（提交时）

| 门槛 | 结果 |
|---|---|
| `./verify.sh` | 全绿：cargo 61+4 / viewer 303 / tools 399 / WASM e2e 无 snap / L1 realism 7 |
| `npx openspec validate --all --strict` | 17/17 |
| D6 逐 seed（`tools/d6-stream-compare.mjs`，seed 1-80 × 90min） | 465,353 事件、516 出界事件；事件数不一致 seed = 0；非出界字段差异 = 0 |
| `cargo test --test realism gm_v1_regression_counts_unchanged` | ok（10 seed × 28 字段 vs v1 基线全等，stream_hash 全变） |
| v1 golden 未被覆盖 | `engine/tests/golden/` 10 文件 git 状态干净；v2 只 streaming hash 变（逐行 diff 已核） |

**out_pos 消费点穷举**（审阅自查）：`grep PassOutOfPlay` 后，读 `out_pos` 的只有两处——
`highlight_ball_end`(lib.rs:966) 与 `finalize_highlight`(lib.rs:1853)，两处均已 clamp01。
无第三消费点，故 variantC 的「两点 clamp」是充分的。

## 3c. 第三轮审阅结论（收尾）

- codex/gpt-5.6-sol：**无 P0/P1/P2**；1 个 P3（`derived-audit.test.js` 的 P21 旧形状注释未标注为兼容 fixture）→ 已改。
- claude/fable-5：**无 P0/P1**；1 个 P2（`viewer/app.js` 顶层 import `?v=` 与 index.html 失同步，
  违反仓库自述约定）→ 已修（ea07bab）；2 个 P3 属**既有遗留、非本 change 引入**，见 §5。

## 4. 决策记录（无需再确认）

**Q1（原待确认）→ 用户已批准 variantC**（2026-09-13）：认可「`out_pos` 存真实越界值 + 两个回灌点
`clamp01`、飞行时长用投影点近似、保 D6 逐 seed 一致」。D3 原文「飞行时间/方向计算用真实终点」的偏离
是**设计级变更，已获批准**，不是实现遗漏。design.md D3/D6 已同步此措辞。

（其余 design 条目无歧义，按 D1/D2/D4/D5/D6 + variantC 实施。）

## 5. 超出本 change 范围、留待后续（审阅发现，未修）

1. **phantom MODIFIED 需求名不止 p27**：`openspec validate --all --strict` 全绿**不等于** change 可归档——
   validate 不校验 MODIFIED/REMOVED 的需求名是否真实存在于主 spec。实测下列开放 change 有同类问题，
   `openspec archive` 会在对应 header 上 abort：
   - `p7-frequency-and-match-clock`（match-engine: 比赛默认时长；pitch-viewer: 比赛时间显示）
   - `p3-continuous-playback`（match-engine: 事件驱动时间推进 / 无球跑位事件 / 进球时间偏移 / carrier_from 对称刷新；
     pitch-viewer: 连续播放比赛 / off_ball_run 演绎 / 连续模式重复段处理）
   - `p4-parallel-beats`（event-stream-protocol: tackle 字段定稿）
   （p27 这一处已在本 change 修复。）
2. **`openspec/specs/match-engine/spec.md` 的 pass result 枚举仍是三值**（success/intercepted/lost），
   漏 `out`/`contested`，与 event-stream-protocol 侧的五值不规范不一致。这是本 change 之前就有的
   状态；因 match-engine 不在本 change 的 Modified Capabilities 清单内，未扩张范围去改。
3. `openspec/changes/p10-match-observation-diagnosis/implementation.md:19` 仍写 unforced_out 排除位含
   `contested`（P21 已移除）。历史实施记录，P21 时即过时，与 P27 无关。

## 6. 全部完成（收尾）

**状态：实现完成、审阅闭环通过（三轮、每轮两个独立零记忆 agent，无遗留 P0/P1）、所有门槛全绿。**

| 门槛 | 最终结果 |
|---|---|
| `./verify.sh` | 全绿（cargo 61 + realism 4 / viewer 304 / tools 400 / WASM e2e 无 snap / L1 realism 7） |
| `npx openspec validate --all --strict` | 17/17 |
| `openspec archive`（/tmp 副本实测） | 成功（+0 ~3 -0；`2026-09-13-p27-out-of-play-protocol`） |
| D6 逐 seed（`tools/d6-stream-compare.mjs`，seed 1-80） | 465,353 事件 / 516 出界；事件数不一致 seed = 0；非出界字段差异 = 0 |
| D6 回归测试（`gm_v1_regression_counts_unchanged`） | ok（10 seed × 28 字段 vs v1 全等，stream_hash 全变） |
| v1 golden | `git diff a23be9a..HEAD -- engine/tests/golden` 为空（一字未改） |
| 缓存版本号 | index.html 与 app.js 10 处 import 均为 `20260913-1` |

**未 push、未合 PR、未动 GitHub issue**（按要求留主 session 监督执行）。

**交付物**：engine（Event 字段 + 三条出界路径 + variantC clamp + 3 条测试）/
viewer（protocol 校验 + 标签 + derive 米制化 + 5 条测试）/
tools（detector D4 优先级 + 契约清单 + D6 对比工具 + fixture 重生成）/
golden-v2（10 seed 新基线）/ OpenSpec（delta spec + design/tasks 同步 + 归档可用）。

**唯一设计偏离**：D3「飞行用真实终点」→ variantC（用户已批准）。见 §2/§4。
