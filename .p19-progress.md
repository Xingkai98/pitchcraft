# P19 进度归档：固定比赛时长（时长保留为参数，UI 不暴露切换）

分支：`p19-fixed-match-duration/2026-09-10`（基于 main @ dc1eafc P19 立项）
状态：实施 + 验证 + 审阅闭环完成。

## 起点说明

worktree 初始分支**落后 main 一个 commit**（缺 P19 立项 commit dc1eafc，change 文档在 main 上）。
分支无自有 commit，故先 `git merge --ff-only main` 快进到 dc1eafc，再开工。

## 目标

把页面比赛时长从「UI 可切换 5/10/45/90 分钟」改为「单一可配置参数、UI 不暴露切换」，
当前值 5 分钟。**关键约束：时长保留为参数**——改 `config.js` 即改时长，不是把 5 写死进
代码逻辑。（实测背景：不同时长不是同一场比赛截长/截短，引擎按 `match_duration_seconds`
重塑整条随机流——seed=42 下 5 分钟 330 事件 vs 90 分钟 5817 事件。）

## 改动文件

| 文件 | 改动 |
|---|---|
| `viewer/config.js` | `playback` 的 `matchDurations: [5,10,45,90]` + `matchDuration: 90` 双字段 → 单一 `matchDuration: 5`；注释注明「改这里即改比赛时长，界面不提供切换」；块头注释把 P19 时长参数与 P7 跳过机制的出处分开标注 |
| `viewer/app.js` | 删 `btnDuration` 引用（原 :62）、`durationIndex`（原 :130-131）、`rebuildGame`（原 :344-348）、`btnDuration` click 监听（原 :357-361）；`init` 两处读时长收敛为 `config.playback.matchDuration`；顶层 9 条 import + `VIEWER_SOURCE_REVISION` bump `20260910-2 → -3`；init 头注释点明时长来源 |
| `viewer/index.html` | 删 `#btn-duration` 按钮；`?v=` `20260910-2 → -3` |
| `viewer/dom-test-harness.mjs` | 新增 opt-in 假 WASM 引擎（`createAppHarness({fakeEngine:true})` + `harness.engineCalls`），默认不装、既有用例零影响 |
| `viewer/app.test.js` | 新增 4 例 P19 测试（见下）；JSDoc 同步 |
| `openspec/changes/p19-fixed-match-duration/tasks.md` | 勾选 P1/P2/P3 |

未碰：引擎（`match_duration_seconds` 参数保留）、服务端、事件流协议、main checkout。

## 新增测试（`viewer/app.test.js`，走真实 app.js + 真实 index.html DOM）

1. **播放控制区无时长切换入口** — 三重断言：`#btn-duration` 不存在；`#controls` 按钮 id
   白名单快照（新增控件必须先登记）；控制区不得含 `select/option/range/number`；控制区
   任何控件文本不得匹配 `/时长|比赛\s*\d+\s*分钟/`。
2. **时长收敛为单一参数** — 经 app.js 实际使用的 config 模块实例断言 `matchDuration === 5`、
   `matchDurations === undefined`。
3. **app.js 用 config 调引擎（非写死）** — 假引擎抓拍，断言 `match_duration_seconds` 等于
   config × 60、seed=42、demo_mode=false。
4. **改 config 即改时长** — 真的把 config 改成 90 后重新初始化，断言引擎收到 5400 秒。

## 测试结果

- `./verify.sh` **全绿**：cargo test 2 passed / viewer **263 passed, 0 failed** / WASM e2e
  「5817 事件 … 无 snap」/ realism L1 7 passed。
- `npx openspec validate --all --strict`：**17 passed, 0 failed**。
- 稳定性：`app.test.js` 与全量 viewer 套件连跑多次，计数稳定无抖动。

## 变异记录（全部真红，测毕还原；worktree 无残留）

| 变异 | 变红用例 |
|---|---|
| app.js 写死 `durMin = 5` | 改 config 即改时长 |
| app.js 写死 `durMin = 90` | 用 config 调引擎（非写死） |
| 加回 `#btn-duration` 按钮 | 无时长切换入口 |
| 换 id 塞「比赛 5 分钟」按钮 | 无时长切换入口（白名单） |
| `<select>` / `<input type=range>` / `<input type=number>` 塞进控制区 | 无时长切换入口（选择器断言） |
| 劫持白名单内按钮 id 换文案「比赛 5 分钟」 | 无时长切换入口（文本断言） |
| `<div id="dur-switch">比赛 5 分钟</div>` | 无时长切换入口（文本断言） |
| config 加回 `matchDurations` 数组 | 时长收敛为单一参数 |
| 删掉/改坏 config 还原逻辑 | 尾部探针 / 用 config 调引擎 |

## 审阅闭环结论（4 轮，独立 paseo agent，各自零记忆）

- **round 1**（审 `git show cdaa210`）：D1/D2/D3 逐条通过，**无 blocker**。留一条实质 nitpick：
  两例 P19 测试都抓不住「app.js 把时长写死成 5」（断言「等于 config × 60」时 config 恰好是 5，
  写死同样成立），审阅实测该变异全绿。根因：harness 的 fetch 桩默认抛错 → engine 恒为 null →
  init 走 mock 分支 → config→引擎那段接线从未执行。
- **round 2**（审 `c7a26ab`）：确认 r1 修复有效（写死 5/90 必红、假引擎抓拍经探针验证非橡皮
  图章、双重 close 幂等、默认路径零影响——改动前 14 例在改后 harness 上全绿），**无 blocker**。
  三条 nitpick：入口断言偏窄（4 种换皮入口溜过）、config 还原无护栏、JSDoc 未同步。
- **round 3**（审 `cd096d4`，收尾轮）：**无 blocker**。26 个变异实测，确认三重断言各守互不
  重叠的入口类别、兜底还原可证伪且承重、无抖动无越界。一条建议：r2 的 `/时长/` 丢了 r1 的
  `/比赛 N 分钟/` 一半。
- **round 4**（审 `1b42ab0`，最终确认轮）：**通过，无 blocker**。确认该改动纯测试、未碰产品代码
  （故 `?v=` 不需再 bump）；合并后的正则忠心覆盖两半；实测旧正则 `/时长/` 至少放过 4 种
  「比赛 N 分钟」换皮（劫持 `btn-toggle`/`btn-replay` 文案、`<div>` 换皮、无空格写法），新正则
  全红且无误报；`\s*` 是覆盖 `比赛5分钟`/`比赛 5 分钟` 两种间距的唯一写法；无 ReDoS 风险。

每轮发现的问题均已修复并复测，修复轮次 commit 见下。

## commit 清单

| commit | 内容 |
|---|---|
| `cdaa210` | P19 实施：固定比赛时长（时长保留为参数，UI 不暴露切换） |
| `c7a26ab` | P19 审阅修复 r1：补「改 config 即改时长」端到端覆盖（堵住写死 5 的漏检） |
| `cd096d4` | P19 审阅修复 r2：加宽 UI 入口断言 + config 还原兜底 + JSDoc 同步 |
| `1b42ab0` | P19 审阅修复 r3：换皮文案断言合并两半（补回「比赛 N 分钟」模式） |
| （本分支末条）| P19 审阅修复 r4：断言提示语同步 + 本进度归档（含一处 r3 commit message 更正，见下） |

## 一处已更正的表述（诚实记录）

r3 commit message（`1b42ab0`）里写「劫持白名单内既有按钮 id 换文案……两类均真红」——这句
**表述过头**。审阅 r4 实测：劫持 `btn-toggle`/`btn-replay` 确会红，但劫持 `btn-speed`/`btn-skip`
是 GREEN，因为 `init()` 成功路径会用 `app.js` 自己的值覆盖这两个按钮的 `textContent`（HTML 里
写的静态文案在断言执行时已被覆盖）。这是**死变异而非漏检**——被覆盖的文案用户看不到，不构成
真实时长入口；审阅另用「改 app.js 自身让它运行时写 `比赛 N 分钟`」的活体测试证明了该模式的
抓取能力。结论（合并是真改进）仍成立，只是措辞需按此理解。

## 遗留 / 备注（非缺陷）

- 入口断言的守备范围止于 `#controls`（spec 场景本身也限定在「播放控制区」）；区外的换皮
  入口不在覆盖内，属已知取舍。
- 控制区按钮 id 白名单对新增按钮是 fail-loud（加控件需同步登记一行），刻意为之。
- 既有债（非本 change 引入）：根 `CLAUDE.md` 的 `?v=` 示例仍写 `20260805-1`。
