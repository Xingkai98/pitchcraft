# P16 进度：viewer DOM 测试基建（jsdom harness）

分支：`p16-viewer-dom-test-harness/2026-09-10`（worktree）
最终 commit：（见文末，审阅修复后回填）

## 落地第一步结论（硬门槛）

`npm install jsdom` **成功** → 走 jsdom 路线，无需回退手写 stub。
- Node v22.22.2，npm 10.9.7，registry npmjs.org 可达。
- jsdom **30.0.1**，38 个包（全为 jsdom 传递依赖），安装 ~10s。
- `module.registerHooks` 在 Node 22.22.2 可用（Node ≥ 22.15 起）。

## 改动文件

| 文件 | 说明 |
|------|------|
| `package.json`（新） | `type: module`、`devDependencies.jsdom`、test script。项目首个第三方依赖，仅测试用 |
| `package-lock.json`（新） | 锁 jsdom 30.0.1 及其传递依赖 |
| `.gitignore` | 加 `node_modules/` |
| `viewer/test-query-loader.mjs`（新） | 查询串 import 剥离 loader（design D2 推荐路线） |
| `viewer/dom-test-harness.mjs`（新） | jsdom DOM harness（建 DOM + stub 浏览器 API） |
| `viewer/app.test.js`（新） | 8 用例，驱动真实 app.js 接线 |
| `verify.sh` | viewer 单测步加 jsdom 预检提示 |
| `openspec/changes/p16-viewer-dom-test-harness/tasks.md` | 勾选 P1–P5.2 完成 |

**`viewer/app.js` 未改动**（`git diff --exit-code viewer/app.js` 通过，byte-identical to HEAD）。测试测的是 app.js 自己的接线。

## 测试结果

- `cd viewer && node --test app.test.js` → **8 pass / 0 fail**（连跑 3 次稳定，无 flaky）
- `cd viewer && node --test *.test.js` → **251 pass / 0 fail**（基线 243 + 新增 8）
- `./verify.sh` → **4/4 步全通过**（Rust cargo test / viewer 单测 251 / WASM e2e / realism L1 7 passed）
- `npx openspec validate --all --strict` → **17 passed / 0 failed**

### app.test.js 用例（8）

1. harness 自检：import 真实 app.js 完成 init（mock 事件流）+ 按钮就位
2. 采集后清空（spec Scenario 1）
3. 提交覆盖（spec Scenario 2）
4. 提交后清空（spec Scenario 3）
5. 提交后清空：等值裁剪/抹除残留文本也无条件清空
6. 凭证抹除：`sk-ant-` 在提交前抹为 `[REDACTED]`（design D4-4）
7. 保留 frozen：采集前输入、采集后未再输入 → 提交保留冻结值（design D4-5）
8. 跨观察隔离：第二条采集不继承第一条描述（P15 错位 bug 整体回归）

### Mutation 验证（证明测试真能拦住回归）

- 删掉 app.js 两行 `obsStatementEl.value = ''`（采集清空 + 提交清空）→ **5 用例红**（2/4/5/7/8）。
- 把 `resolveSubmitStatement(lastBundle.statement, obsStatementEl.value)` 第二参改成 `''` → **3 用例红**（3/6/8）。
- 两次均 `git checkout` 还原，`git diff --exit-code viewer/app.js` 干净。

## Loader 选型（design D2）

选 **方案 1（`module.registerHooks`）**：resolve 阶段把 `./x.js?v=…` 解析成「保留查询串的 file URL」，load 阶段剥查询串 readFileSync。
- **不改 app.js 源码**，浏览器 cache-busting 纹丝不动（浏览器侧无感知）。
- 保留查询串 → 不同查询串是不同模块 URL（与浏览器语义一致），因此每个用例可 import 一份**独立的 app.js 模块实例**，模块级状态（lastBundle / observationList / game）跨用例不串味 —— 这是 per-test 隔离的关键。
- 不误伤 node_modules 裸包名（只接管 `./` `../` `/` `file:` 形态）。
- Node < 22.15 时抛明确错误提示升级（不静默回退到「改 app.js」方案）。
- 全仓库仅 `app.js` 顶层 import 带 `?v=`，传递依赖不带，故无需变体处理。

## stub 面（design D3）

- **Canvas**：`HTMLCanvasElement.prototype.getContext` → 复用 `mock-canvas.js` 的 `MockContext`（renderer.test.js / micro-motion.test.js 同款）。
- **WASM**：**走 mock-event-stream 路径**（design D3 优先项）。`fetch` 桩默认抛错 → app.js 的 `tryLoadEngine` catch 后退回 `mockEventStream()`，全程不碰 engine.wasm、无网络。
- **rAF**：no-op。渲染循环属 design.md Non-Goals，no-op 保证确定性、无空转定时器。
- **全局**：`document`/`window`/`location`/`localStorage`/`navigator`/`getComputedStyle` 从 jsdom window 借入 globalThis，`close()` 时按原描述符**逆序还原**，不污染同进程其他测试文件。
- **fetch 桩**：记录调用（url/method/解析后的 body），测试据此断言「实际 POST 出去的 bundle」。

## Review 结论

### Round 1（paseo 独立 agent，审 `7bc9416`）：不通过 → 已修

结论「不通过」，2 项必修 + 3 项低危/提示。**两项必修均为「当前用例不失败、但真实存在的隐患」**：

| # | 级别 | 问题 | 修复 |
|---|------|------|------|
| 1 | 必修 | `test-query-loader.mjs` 注册 `load` 钩子（哪怕纯 passthrough）会改变同进程后续经 ESM import 载入的 CJS 包链接：注册后 `import('jsdom')` 报 `ERR_VM_MODULE_LINK_FAILURE`（受影响不止 jsdom：whatwg-url / html-encoding-sniffer / cssstyle）。当前靠 harness 静态 import jsdom 恰在其之前执行掩盖 | **改 resolve-only**：查询串搬到 URL fragment（`#`）→ 路径干净，内置加载器直接读盘，无需 load 钩子；fragment 仅作模块身份 → 隔离语义不变。实测注册后 `import('jsdom')` 恢复正常（commit `e43e3fd`） |
| 2 | 必修 | `dom-test-harness.mjs` 全局 patch/restore 假定 harness 永不重叠；重叠时 close() 逆序还原互相踩踏，静默留下指向已销毁 window 的悬空全局 | 新增**单例守卫**：同时只允许一个 harness 存活，重叠创建抛明确错误（commit `e43e3fd`） |
| 3 | 低 | `close()` 未清 app.js `showNotice` 定时器 → 拖慢退出（app.test.js 4.4s）+ 往 detached DOM 写 | harness 包一层 `setTimeout`/`clearTimeout` 记录并清理。**实测未破坏 node:test 超时**（挂死用例仍被 `{timeout:800}` abort）。4.4s → 1.9s（commit `632f8de`） |
| 4 | 低 | `FORMAT_BY_EXT` 属投机泛化 | 随必修 1 的 resolve-only 改法消失 |
| 5 | 提示 | 仓库无 CI 配置，「进 CI」当前只由 `verify.sh` 兑现 | 与仓库现状一致（无 `.github/`），不违约 |

审阅 agent 独立做的 mutation 复验与 tasks.md 声明**逐条吻合**（删两行清空→5 红；改 `resolveSubmitStatement` 接线→3 红；删按钮监听器→7/8 红）。其还确认：app.js 与 HEAD~ 逐字节相同、`npm ls --omit=dev` 为空、lock 38 条全在 jsdom 闭包内、app.test.js 连跑 10 次稳定、`--experimental-test-isolation=none` 下 251 仍全绿。

### Round 2（paseo 独立 agent，审 `632f8de`）：**通过（无遗留必修问题）**

必修项：无。审阅 agent 逐条实测复验了两项必修修复：

- **必修 1 真修好**：注册当前 loader 后 `import('jsdom')` 成功；同进程注册旧 passthrough
  `load` 钩子则复现 `ERR_VM_MODULE_LINK_FAILURE`（因果链验证）；resolve 产出
  `file:///…/observation.js#v=20260910-1`（路径干净）；`?one`/`?two` 仍得独立实例；
  裸包名不误伤。
- **必修 2 真修好**：重叠创建抛明确错误；close() 后 11 个全局与基线描述符逐一全等；
  close 后可再建、重复 close 幂等。
- **低 3 修法安全**：node:test `{timeout:800}` 仍正常 abort 挂死用例；`clearTimeout` 语义
  透传；运行期非 app.js 来源定时器计数 = 0；close 后活跃 Timeout = 0，进程 393ms 退出。
- mutation 三方复验与声明**完全一致**（5 红 / 3 红 / 7 红）；`git diff e96eb95 HEAD --
  viewer/app.js viewer/index.html` = 0 行；`verify.sh` 第 2 步在红测试下退出码 1（pipefail 生效）；
  lock 38 条全在 jsdom 闭包内；`node --test app.test.js` 连跑 5 遍稳定。

**Round 2 提出者（低危/提示），均已处理：**
- L1（低危，已修）：`tasks.md` P2.1 的描述仍写「load 阶段剥查询串读盘」（r1 前的旧实现），
  与代码及 loader 头注释矛盾 —— 已改为描述 resolve-only + fragment 方案。
- I1（提示，未改）：`setTimeout` 包装理论上会清掉测试自身排的定时器；实测非 app.js 来源
  计数 = 0，无实际影响。
- I2（提示，已加注释）：只有 `app.js` 逐用例隔离，其传递依赖共享实例 —— 已实测无模块级
  可变状态受影响，并在 `dom-test-harness.mjs` 写明该边界供后来人参考。

## 最终 commit

| commit | 内容 |
|--------|------|
| `7bc9416` | P16 实施：viewer DOM 测试基建（jsdom harness，锁 P15 采集/提交行为） |
| `e43e3fd` | P16 审阅修复 r1：loader 改 resolve-only（消 CJS 链接隐患）+ harness 单例守卫 |
| `632f8de` | P16 审阅修复 r2：harness 清理 app.js 遗留的 showNotice 定时器 |
| `11ead22` | P16 审阅收尾 r2：任务文档对齐 resolve-only 实现 + 补记隔离边界 |

基线：`e96eb95`（P16 立项）。**`viewer/app.js` 全程未改动**（`git diff e96eb95 HEAD -- viewer/app.js` 为空）。

### 交付物清单

新增：
- `package.json` / `package-lock.json` — jsdom 30.0.1 devDependency（项目首个第三方依赖）
- `viewer/test-query-loader.mjs` — 查询串剥离 loader（resolve-only + fragment）
- `viewer/dom-test-harness.mjs` — jsdom DOM harness
- `viewer/app.test.js` — 8 用例

修改：
- `verify.sh` — viewer 单测步加 jsdom 预检
- `.gitignore` — `node_modules/`
- `openspec/changes/p16-viewer-dom-test-harness/tasks.md` — P1–P5.3 全部勾选

### 最终验证（全绿）

```
viewer/app.test.js      8 pass / 0 fail
viewer 单测（全量）     251 pass / 0 fail   （基线 243 + 新增 8）
./verify.sh             4/4 步全通过
openspec validate       17 passed / 0 failed
```

### 未决 / 说明

- **无 open question**（design 已定，本 change 全部按 design 落地）。
- 仓库无 `.github/` CI 配置，spec 里「随 CI 运行」当前由 `verify.sh`（唯一入口）兑现 ——
  与仓库现状一致，非本 change 引入的缺口。
- `jsdom` 仅 devDependency，未进运行时/WASM/浏览器 bundle（`npm ls --omit=dev` 为空，
  `viewer/index.html` 未引用新文件故无需 bump `?v=`）。
