# P26 进度：player_overlap 同队间距 detector

分支：`p26-player-overlap-detector/2026-09-13`（立项 commit ac43b52）

## 现状调研（已完成）

- 环境：`npm install` 完成（jsdom）；WASM 已构建到 `viewer/engine.wasm`（engine 无改动，形状与落盘 fixture 一致）。
- 现有 4 个 detector 在 `tools/detectors.mjs`：`detectInvariants` / `detectUnforcedOut` /
  `detectInactiveResponsibility` / `detectIgnoredInterception`，全部导出（契约测试的逐条目 runner 需要）。
- `DETECTOR_PROFILE_KEY`（detectors.mjs:79）把 detector_id 映射到 profile 配置块；新 detector 必须登记，
  否则 `isUncalibrated` 返回 true（fail-closed，不会误升级 failure，但应在 profile 里显式声明 calibrated）。
- `runAudit` 里：findings 数组拼接 → 挂 profile_id/version → 未标定标记 → `statsFor(...)` 逐 detector 产 stats。
- 契约 `tools/detector-field-contract.mjs`：每条目 reads/legacy_reads/producers/known_gaps/notes；
  `detector-field-contract.test.mjs` 的 `DETECTOR_ENTRY` 必须为每个契约条目配一个**逐条目 runner**
  （否则 "every contract entry has a drift-guard runner" 红）。
- `detectors.test.mjs` 末尾有 **golden finding 签名** `GOLDEN_FINDINGS`（7 窗口全量 finding 逐条），
  新增 detector 会改真实窗口 finding 集合 → 必须同步更新（这正是该守卫的预期代价）。
- 新增 detector 还会命中 `detector-field-contract.test.mjs` 的
  "contract covers every detector"（硬编码期望列表）与源码级扫描（reads 必须在契约里）。

## 真实 fixture 实测（用当前落盘 fixture 算的）

同队 pair 的窗口内最小间距（阈值 2.0）：

| 窗口 | 同队 pair | 最小间距 | 首次越界 t | 采样点 |
|---|---|---|---|---|
| corner | home 4/5 | **0.434** | 1566 | 21（对齐） |
| clearance | home 1/2 | 5.459 | — | 21（对齐） |
| free_kick | home 6/9 | 9.885 | — | 21（对齐） |
| out_goal_line | home 1/2 | 12.725 | — | 21（对齐） |
| throw_in | away 16/17 | 30.257 | — | 21（对齐） |
| out_sideline | 无同队 pair（9/19 跨队） | — | — | — |
| whistle_dead_ball | 无同队 pair（0/17 跨队） | — | — | — |

→ **真实 fixture 上只有 `corner` 窗口报 1 条**（home 4/5，0.434m）。告警量正常，不刷屏
（每个窗口子集只有 2 名球员，无 design 担心的 >100 条问题）。

## 实现计划

1. **P1（测试先行）**：先写测试（P3 的用例 + 更新 golden 签名），跑出红。
2. **P2**：`tools/detectors.mjs`
   - `DEFAULT_AUDIT_PROFILE` 增 `player_overlap: { min_distance: 2.0, calibrated: true }`
     （D4 显式声明；位置重叠是确定性规则、无需真实比赛标定 → true，与 invariants 同理）。
   - `DETECTOR_PROFILE_KEY` 增 `player_overlap: 'player_overlap'`。
   - `export function detectPlayerOverlap(players, profile)`（D6 t 对齐 / D1 id 范围 team / D2 pair 聚合 / D3 realism_warning）。
   - `runAudit` 集成：拼接 findings + `statsFor('player_overlap', <pair 数>, findings)`。
3. **P2b**：`tools/detector-field-contract.mjs` 增 `player_overlap` 条目
   （reads `['t','x','y']`，producers `t:'viewer', x:'derive', y:'derive'`，known_gaps: []）。
4. **P3**：`tools/detectors.test.mjs` 增用例 + 更新 `GOLDEN_FINDINGS`；
   `tools/detector-field-contract.test.mjs` 增 `DETECTOR_ENTRY.player_overlap` runner +
   "contract covers every detector" 期望列表。
5. **P4**：`./verify.sh` 全绿 + `npx openspec validate --all --strict`；
   独立 paseo agent 审阅闭环。

## 实施结果（已完成 P1-P4.1）

- `tools/detectors.mjs`：新增 `detectPlayerOverlap(players, profile)` +
  `teamOfPlayerId` / `sameTeamRoster` / `sameTeamPairCount`；DEFAULT_AUDIT_PROFILE 增
  `player_overlap: { min_distance: 2.0, calibrated: false }`（D4 未标定，见下）；reference_bands 增条目；
  `DETECTOR_PROFILE_KEY` 登记；runAudit 集成 + statsFor（样本口径 = 同队 pair 数）。
- `tools/detector-field-contract.mjs`：增 `player_overlap` 条目（reads t/x/y）。
- `tools/detectors.test.mjs`：增 8 条用例 + GOLDEN_FINDINGS 增 player_overlap 行。
- `tools/detector-field-contract.test.mjs`：增 `DETECTOR_ENTRY.player_overlap` runner + 期望列表。

**关于 D4 的标定标记取舍（design 未明确，实现判定，请审阅者复核）**：
`calibrated` 机制管的是「告警**率**是否用真实比赛标定过」（P21 D4 / #36），不是「阈值是否可信」。
`min_distance: 2.0` 是真实比赛常识，但告警率确实没标定过，所以与 `ignored_interception` 同样标
`calibrated: false`：finding 带 `calibrated:false`、聚合层不因超 band 升级 `realism_failure`，
**保住 D3 的 `realism_warning` 语义**。若标 `true`，则 `reference_bands.player_overlap.max` 一旦
被谁设成数字就会升级 failure——那不是 D3 要的。

## 自测发现并修复的缺陷（1 条 P1）

- **非有限 t 不构成可对齐采样点**：原实现只判 `typeof t === 'number'`，`t: NaN` 会被收进采样表，
  与另一名球员的 `NaN` 键「对齐」产出一条 `match_time: null`（NaN JSON 化）的**假 finding**。
  修法：只收 `Number.isFinite(t)`。已加回归测试（并发现第一版回归测试是假绿——有真实 t 兜底时
  抓不住变异，已单独补「纯非 NaN 对齐」用例）。

## 自测变异结果（测试有牙证明）

M-B `<`→`<=`、M-C team 范围 0-11/12-21、M-D min_distance 取首次越界值、M-E 缺采样当 (0,0)、
M-F match_time 取最后越界、M-G severity 改 invariant_violation、M-I 去 pair 聚合、M-J statsFor
口径改球员数 —— **全部变红**。M-H（finding id 带 t）不变红：id 里带 t 不影响「只报一条」，
逐采样点刷屏由 M-I 兜住，判定为可接受（若审阅认为 id 稳定性也该钉，按 P2 处理）。

## 门槛结果

- `./verify.sh`：全绿（cargo 3 passed / viewer 297 / tools 392 / WASM e2e OK / realism L1 7 passed）。
- `npx openspec validate --all --strict`：17/17 通过。
- 真实 fixture 实测：仅 `corner` 窗口报 1 条（home 4/5，min 0.434m @t=1560），告警量正常不刷屏。

## 待确认问题

（暂无。审阅者若对 D4 的 `calibrated: false` 取舍有异议，请在此记录。）
