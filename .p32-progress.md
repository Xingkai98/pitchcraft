# P32 进度：ignored_interception 假阳性治理（detector 删除 + 引擎自洽硬门，修 #36）

分支 `p32-ignored-interception-calibration/2026-09-14`（立项 `9add879`）。
方案定稿：`openspec/changes/p32-ignored-interception-calibration/{proposal,design,tasks}.md` +
`reviews/grill-design.md`（Q1-Q10 全按 codex 推荐，用户已确认）；spec delta 两份
（`match-engine` ADDED + `diagnosis-runner` MODIFIED）。

## 立项理由（一句话）

`ignored_interception_opportunity` detector 实测 **45% 假阳性**（issue #36）——根因不是参数不准，
而是它的「防守者能跑到走廊就该拦」是**确定性语义**，与 post-#25 的**概率拦截引擎**
（贴防 7.5% / 中距 4.5% / 远距 2.0% + 长传加成，cap）根本错位。且引擎有第二条开放比赛传球路径
`emit_forward_pass_highlight`（产 `result:"success"` 但从不走拦截 roll、事件形状与普通成功传球
逐字段一致），audit 层无法重建无偏分母。**调参救不了** → 删 detector，把自洽硬门做进引擎侧。

## 实现（P1-P3）

### P1 引擎侧拦截自洽硬门（`engine/src/lib.rs`）

- 记账点 = `emit_pass_highlight_inner` 的拦截判定处，严格插在 `fail_roll = rng.next_u64() % 100`
  **之后**、三选一分支之前。**零 RNG、零事件流写入** → golden-v5 逐字节不变（实测 `gm_canary_seeds` 绿）。
- `OpportunityTally` 新增：`intercept_roll_{samples,expected,actual}`、`intercept_bucket_*[3]`、
  `intercept_long_*`、`intercept_very_long_*`、`intercept_bucket_long_*[3]`、`intercept_bucket_very_long_*[3]`。
- **整数 roll 量化**（本 change 的核心口径）：判定 `fail_roll < interception_p` 是 [0,99] 整数比较，
  故命中 `ceil(interception_p)` 个 roll 值（7.5→8、4.5→5、2.0→2、cap 60→60）。期望拦截数 =
  Σ`ceil(p)`/100。**不得**拿浮点常量 ÷100（会系统性下偏）。
- 抽纯函数 `open_play_interception_p` / `interception_roll_width` / `interception_distance_bucket`
  + `hits_long_pass`/`hits_very_long_pass`（取值与 RNG 顺序逐字不变）；`min(60.0)` 字面量提为
  具名常量 `INTERCEPT_P_CAP`。
- 新测试 `p32_interception_self_consistency`（200 seed）+ `p32_interception_roll_width_quantizes_by_ceil`。

### P2 detector 彻底删（不是降级壳）

`tools/detectors.mjs`（函数 / profile 块 / reference_band / DETECTOR_PROFILE_KEY 映射 / runAudit
集成 / stats）、`tools/detector-field-contract.mjs` 条目、两份测试的用例全删。未标定降级机制回归
样本改用 `player_overlap`；golden 签名删 6 行 `ignored_interception_opportunity`。

### P3 pass_outcomes 软分层（不产 finding、不进 band）

`computePassOutcomes` 新增 `strata: {high_ball, low_ball, unknown_h, long_pass, short_pass,
restart_type, open_play}`；`classifyPassOutcome` 识别 `intercepted`/`lost`（此前落 unknown_outcome）。
`mergePassOutcomes` 只**加** strata 键，既有四桶 + `total_ordinary_passes` 不变，旧 report 无 strata
按零计。

## 关键实测（200 seed × 90min，debug）

```
拦截 roll 总样本 70720  期望 5263.0  实际 5260
桶样本 贴防 23955 / 中距 45536 / 远距 1229（全 ≥ 200，逐桶 95% CI 全过）
贴防桶三层：非长传 8.00（基线） / 长传 15.04 / 超长 23.00 —— 加成接线逐常量绑死
真实档位上界 = 7.5 + 7 + 8 = 22.5（cap 60 是防御性上限、生产不可达）
```

## 变异验证（守卫真绑接线，非「恰好通过」）

删记账 → 红；`ceil`→截断 → 红；`LONG_PASS_INTERCEPT_BONUS` 归零 → 红；
`VERY_LONG_PASS_INTERCEPT_BONUS` 归零 → 红。（自洽测试 + 纯函数测试双红，多次复现）

---

# 审阅闭环记录（CLAUDE.md 强制收尾）

两轮独立零记忆 subagent（Opus）审阅，全部真跑测试 + 变异。

## R1（对象 `7f174e9`）：**无 P1，2 P2 + 7 P3**；12 条核心声明全验真

独立验证为真：detector 彻底删（全仓 grep）、整数 roll 量化、记账零 RNG/零事件流、golden 不变
（自跑）、分桶/常量同源、CI 量纲正确（旧版 100× 错未复现）、变异可杀死接线、viewer 生产不动、
strata 软参考、主 spec/delta 逐字对齐（archive 盲区专项通过）、字段契约自洽、样本量声明属实。

| ID | 问题 | 处置（commit `0e5c8de`） |
|---|---|---|
| P2-1 | tasks.md 引用不存在的 reviews/review.md | 补齐该文件 |
| P2-2 | tasks.md P4 三项未勾选 | 如实勾选 + 补实测数字 |
| P3-1 | CI 方差注释误称等式（实为 Cauchy–Schwarz 上界，偏宽保守） | 注释改「保守上界」+ 给 0.8% 偏差 |
| P3-2 | `tight_very_long_n` 未使用告警 | 用于分桶减法口径断言 |
| P3-3 | `intercept_long/very_long_actual` 只写不读 | 加活性断言（actual ≤ 样本、very ⊆ long） |
| P3-4 | spec 的 insufficient_sample 无处落地 | 改 `Err` 返回值 + eprintln（非静默） |
| P3-5 | cap scenario 生产不可达 | spec/design 明说 cap 是防御性上限、真实上界 22.5 |
| P3-6 | `RESTART_DETAILS` 字面量拷贝 + 与 design 的 goal_kick 冲突 | 改 `= EXCLUSION_DETAILS`；design 记偏离 |
| P3-7 | 缺失 h 并入 low_ball（high_ball 率系统性偏低） | 新增 `unknown_h` 层（不伪造） |

## R2（对象 `0e5c8de`）：**通过，可归档**；9 条修复逐条验真 + 无回归

两个重点复核：**A** `hits_long_pass` 替代内联比较逐字等价、RNG 序列未变、golden 绿；
**B** `unknown_h` 不误分类（探针实测真实引擎 8359 条 pass 的 missing_h=0；fixture 里 2 条缺 h 的
out pass 经 git 取证确认是 P27 时点无 h 发射点的陈旧快照，P31 删槽位后生产链路恒带 h）。

R2 新发现 3 条 P3 → 收口于 commit `453e51e`：

| ID | 问题 | 处置 |
|---|---|---|
| N-2 | 只有贴防桶有最小样本断言，次要桶可能静默退化 | 三桶统一硬断言 `samples[b] >= 200` |
| N-1 | insufficient_sample 走 eprintln，默认 cargo test 下被捕获不可见 | 改「先硬断言样本充足，再 `assert!(...is_ok())`」——该状态不可达 |
| N-3 | tasks.md 把两条命令写成一条 | 措辞修正 |

**审阅闭环两轮通过，无遗留。** 变异复验（refactor 后）：删记账 → 红、LONG 归零 → 红。

## 收尾状态

`./verify.sh` 五步全绿（119 lib 测试 + golden 4/4 + viewer 303/303 + tools 404/404 +
WASM e2e 5817 事件 + realism release 8/8）+ `npx openspec validate --all --strict` 17/17。
golden-v5 未重基线（记账不进事件流 → 逐字节不变）。

## 遗留（不阻断归档，登记为后续）

- **「高球标记率 85%」未复现/否定**：真实 fixture 仅 6 条 pass（h>0 者 1 条），样本远不足以判定。
  本轮交付**分层机制**与对照口径；复现/否定需真实多 seed 全 90min bundle（#36 后续标定）。
- `pass_outcomes.strata` 的 `h` 生产者是引擎直出（derive 层 `{...e}` 直传）；若将来 derive 不透传 h，
  契约测试会红（守卫已覆盖）。
