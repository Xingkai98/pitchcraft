# P18 进度：观察描述以服务端为权威（刷新自动回填 statement）

分支：`p18-observation-statement-refill/2026-09-10`（worktree，基于 main）

| commit | 内容 |
|--------|------|
| `835d97a` | P18 立项（change 文档） |
| `9ec1df4` | P18 实施：服务端 statement + 页面纯函数 + 接线 + 测试 |
| `93f5b89` | 审阅修复 r1：版本注释去写死 + spec 场景旧值具体化 |
| `39f5007` | 审阅修复 r2：补合成响应路径的 statement 断言 + 注释措辞 |

最终实质 commit：`39f5007`（其后仅本进度文档）。

## 问题与目标

`GET /tasks/:id` 的字段白名单里没有 `statement`，页面刷新后 `restoreObservationList` 只回填
status、从不动 statement。于是服务端 bundle 的 statement（诊断 runner 读的权威值）与页面
显示值割裂：P15 错位修复改的是 bundle，页面仍显示 localStorage 旧描述，只能手动清
localStorage 才同步。

本 change 让已提交（有 task_id）条目的 statement 以服务端 bundle 为权威，刷新/轮询自动回填。

## 改动文件

`git diff --name-only 835d97a..HEAD` 共 11 个文件。

| 文件 | 改动 |
|------|------|
| `tools/service.mjs` | `readTaskState` 响应加 `statement: readBundle(id)?.statement ?? null`（+5 行） |
| `viewer/observation-list.js` | 新增纯函数 `applyServerStatement(entry, statement)`（+8 行） |
| `viewer/app.js` | `pollTask` 里把服务端 statement 并进 `updateEntry` 的 patch（接线）|
| `viewer/index.html` | `?v=20260910-1` → `?v=20260910-2` |
| `viewer/test-query-loader.mjs` | 文件头注释不再写死版本值（审阅 r1/r2） |
| `tools/service.test.mjs` | +6 用例（含合成响应路径） |
| `viewer/observation-list.test.js` | +4 用例 |
| `viewer/app.test.js` | +4 用例（jsdom harness，走真实 app.js 刷新路径） |
| `openspec/changes/p18-observation-statement-refill/tasks.md` | 勾选 |
| `openspec/changes/p18-observation-statement-refill/specs/match-observation/spec.md` | 场景 GIVEN 旧值具体化（审阅 r1） |
| `.p18-progress.md` | 本文件 |

未碰：task.json schema、POST /observations、引擎（`engine/`）、事件流协议、problems 落盘/生命周期。
（第三轮审阅用 `grep -iE "engine|protocol|task\.json|runner\.mjs|bundle\.mjs"` 核过 diff 文件列表，为空。）

## 关键语义（正确性核心）

1. **空串 ≠ null**：服务端 `readBundle(id)?.statement ?? null` —— `??` 只兜 null/undefined，
   空串（用户清空描述）原样返回。页面侧 `applyServerStatement` 用 `typeof statement === 'string'`
   判断（不是真值判断），空串照样覆盖。两端配合才使「服务端把描述清空 → 刷新后页面也清空」
   成立。任何 `||` / `if (statement)` 式的写法都会把这个语义打破——审阅轮 1/2/3 各做过一次
   「`??` 改 `||`」「`typeof` 改真值判断」的变异，对应用例均精确变红。
2. **终态条目也回填**：回填位置在 `pollTask` 里 `if (isTerminalStatus(state)) { ...; return; }`
   **之前**，故终态条目刷新时同样回填（否则「刷新看服务端改后的描述」这个主场景恰好失效，
   因为刷新时任务多半已是终态）。
3. **两条路径共用同一处**：`restoreObservationList`（刷新恢复）与 `submitObservation` 提交后的
   实时轮询都走 `pollTask`，无需分别接线。
4. **每次轮询迭代都回填**：回填在 `while` 循环体内（非只在首次），服务端中途改描述能被后续
   轮询捡到；值未变时短路不写 patch（不产生额外的 localStorage 写放大）。
5. **无 task_id 不参与**：`restoreObservationList` 只对 `task_id` 非空的条目调 `pollTask`，
   采集未提交条目保持本地值。

## 版本号处理

按 `viewer/app.js` 文件头既有约定（「改 JS 后统一更新」）与 P15 审阅先例（commit `c0bb824`：
审阅明确修过「只 bump 了改动的那一行 import」这一问题），本次顶层 import 版本号**全量** bump
`20260910-1` → `20260910-2`，与 `index.html` 的 `?v=`、`VIEWER_SOURCE_REVISION` 三处一致。
全仓库 grep 无残留 `20260910-1`。

## 测试结果

| 范围 | 命令 | 结果 |
|------|------|------|
| 服务端 | `cd tools && node --test *.test.mjs` | 286 通过 / 0 失败 |
| viewer | `cd viewer && node --test *.test.js` | 259 通过 / 0 失败（含 app.test.js 12 例） |
| 全量分层 | `./verify.sh` | 全部验证通过（cargo test + viewer 259 + WASM e2e 5817 事件无 snap + realism L1 7 passed） |
| OpenSpec | `npx openspec validate --all --strict`（仓库根） | 17/17 通过（12 changes + 5 specs） |

> 注：`openspec validate` 需在仓库根跑；在 `tools/`/`viewer/` 子目录跑会报 "No items found"。

### 变异测试记录（证明用例真驱动接线，非空转）

| 变异 | 预期 | 实测 |
|------|------|------|
| 删掉 `app.js` `pollTask` 里的 statement 回填块 | 回填用例红 | `not ok 8 / 9`，其余 10 绿 |
| 把回填块挪到终态 `return` **之后** | 回填用例红（终态不生效） | `not ok 8 / 9` |
| `service.mjs` 的 `??` 改 `\|\|` | 空串用例红 | `not ok 10`（空串 vs 缺失） |
| 删掉服务端 `statement` 字段 | P18 服务端 6 例全红 | `not ok 9–14` |
| `applyServerStatement` 去掉 `typeof` 守卫 | 覆盖语义用例红 | `not ok 10 / 24 / 25` |

每处变异都精确打在 P18 新增用例上、不误伤既有用例。变异后均 `git checkout` 还原并复核
工作区干净。

## 代码审阅闭环（3 轮，全部通过）

用独立零记忆 paseo agent（`claude/claude-fable-5[1m]` + `bypassPermissions`）审，每轮都要求
其自跑验证命令 + 变异测试，而非只读代码。

| 轮 | 审阅对象 | 结论 | 留下并处理的问题 |
|----|----------|------|------------------|
| r1 | `9ec1df4` | 通过，无 blocker | 2 nitpick：`test-query-loader.mjs` 注释写死版本值；delta spec 场景 GIVEN 用「」占位符像「旧值就是空」。均修于 `93f5b89` |
| r2 | `93f5b89` | 通过，无 blocker | 2 nitpick：合成 `auditing` 响应路径的 statement 无测试覆盖（P18 新增字段唯一未被钉住的返回路径）；`test-query-loader.mjs` 注释「文件头」措辞不精确。均修于 `39f5007` |
| r3 | `39f5007` | **通过，无遗留问题**，无新增 blocker/建议/nitpick | — |

被审阅方主动记录为「不改」的两条（两轮均评估为不值得改）：`readTaskState` 让
create-from-report / `importProblems` 路径多读一次 bundle（实测本地 1-3ms/次，加请求内缓存
的收益不抵复杂度）；`9ec1df4` 的 commit message 有个手滑的 `//`（已推历史文本，不改写）。

## 未验证/无法确认的点（如实记录）

- 浏览器侧 `?v=` bump 后是否真正强制重新拉取模块，无真实浏览器端到端验证（仓库既有机制，
  三处版本号已一致）。
- 「终态回填后的 statement 被下游 change 草稿/建问题复用」是调用顺序的必然推论
  （`updateEntry` 在 `renderEntryFeedback` 之前），未写独立断言。

## 最终 commit hash

`39f5007`（审阅修复 r2；P18 实施链的最后一个实质 commit）
