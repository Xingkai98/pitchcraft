# P21 进度：修复 detector 字段契约断裂 + 防再犯

- Change：`openspec/changes/p21-detector-field-contract-fix/`
- 分支：`p21-detector-field-contract-fix/2026-09-12`
- 立项 commit：`04e4f56`
- 实施 commit：（待填）
- 审阅修复：（待填）

## 现状核实（动手前）

已通读四个 change 文档 + `tools/detectors.mjs` + `tools/detectors.test.mjs` +
`viewer/derive-audit-features.js` + `viewer/observation.js` + `tools/runner.mjs`（audit 段）。

**基线测试**（`./verify.sh`，先补 `npm install` + 重建 `engine.wasm`，二者都是 gitignored 产物）：
viewer 288 绿 / tools 321 绿 / cargo test 绿 / WASM e2e 绿（5817 事件，4 seed 无 snap）/ realism L1 绿。
`npx openspec validate --all --strict` → 17/17。

**真实引擎实证**（seed 42，5400s，走 `protocol.parseEventStream → Game → captureObservation`
真实链路，不手写）：

```
pass results: {"success":352,"intercepted":31,"contested":16,"lost":9}
pass details（非 out_*）: {"throw_in":4,"free_kick":26,"corner":5,"clearance":1}
真实出界传球样例:
  {t:244, type:"pass", from:9, x2:73.679, y2:0, result:"contested",
   detail:"out_sideline", pass_distance:27.758, nearest_defender_distance:9.431, ...}
  {t:2340, ..., x2:105, y2:23.12, result:"contested", detail:"out_goal_line", ...}
```

四条断链全部**实测复现**（不是推理）：
1. `result==='out'` 引擎从不产（真实值只有 success/intercepted/contested/lost）。
2. 出界落点被 `clamp01` 钳回场内（`y2=0`、`x2=105` 恰好在边界上）→ `isOutOfPitch`
   严格不等式不成立 → 几何出界分支也失效。**两个信号都瞎** → unforced_out 对真出界
   100% 落 unknown（实测 t=244/2340 两个窗口 unforced_out 全 unknown）。
3. `EXCLUSION_KEYS` 六个布尔键实测**一个都不产**（pass 事件真实键集里无任何布尔排除位）。
4. `target_distance`/`out_reason`/`formation_hold` 实测**无生产者**（pass 键集与快照键集
   里都没有）。快照真实键集：`t,x,y,is_gk,responsibility,responsibility_source,
   moved_toward_goal,moved_toward_ball,dead_ball`（`dead_ball` 有生产者——t=5400 窗口
   198 条；`formation_hold` 确无 → K3 死代码确认）。

## 实现计划

按 tasks.md P1→P5，测试先行（red→green），每阶段落盘 + commit。

### P1 契约清单模块（L2 前置）
- 新增 `tools/detector-field-contract.mjs`：`DETECTOR_FIELD_CONTRACT`（4 个 detector
  + pass_outcomes 聚合桶）+ `AUDIT_INPUT_SCHEMA_VERSION` 再导出。
- **常量落点**：`AUDIT_INPUT_SCHEMA_VERSION` 定义在 `viewer/derive-audit-features.js`
  （生产方、浏览器可 import），contract 模块 `export ... from '../viewer/...'` 再导出。
  理由：viewer 不能 import tools（浏览器静态服务只服务 viewer/），tools→viewer 有先例
  （`tools/runner.mjs` import `viewer/event-labels.js`）。
- reads 形状：`reads`（detector 依赖、必须有人产）+ `legacy_reads`（D2 兼容布尔位，
  明确允许无生产者）+ `known_gaps`（`{id, field, reason, issue}`，`field` 非 null 的
  即「无生产者的读取字段」）。

### P2 L1 修断裂
- D1 `outEvidenceOf`：detail 出界 → `'event.detail'`；`result==='out'` → `'event.result'`；
  几何 → `'landing_out_of_bounds'`；否则 null。`classifyPassOutcome` 复用同一契约。
- D2 `EXCLUSION_DETAILS = ['corner','throw_in','free_kick','clearance']` + 布尔兼容
  `EXCLUSION_LEGACY_KEYS = ['corner','throw_in','clearance']`（删 dead_ball/goal_kick/contested）。
  排除 reason 标签沿用 `excluded: <token>` 形式（布尔命中报键名、detail 命中报 detail 值）。
- D3 `out_reason` 由 detail 推导（out_sideline/out_goal_line/out_of_bounds_landing/no_pressure_out）；
  features 删 `target_distance`。
- D4 `ignored_interception` 降级：profile 加 `calibrated:false`；finding 加 `calibrated:false`；
  `aggregateAudit` 未标定 detector 不做 band 升级（band_state=null）+ 摘要 `calibration:'uncalibrated'`。
- D5/P2.6 删 inactive_responsibility 的 `formation_hold` 死代码 → 登记 K3 known-gap。

### P3 版本保护
- `deriveAuditInput` 输出加 `schema_version`；`runAudit` 入口校验（缺失/未知 → 抛错）。
- `runDiagnosis` 捕获版本错 → `finalize('failed', {failure_kind:'invalid_audit_input'})`
  （与既有 bundle_read_error/invalid_bundle 同模式；避免抛穿 service 后台任务）。
  ※ 这是 design.md 未明写的实现细节，按既有错误处理契约补，会在收尾报告里标注。

### P4 真实 fixture
- `tools/fixtures/real-audit-input.json`：用真实 WASM + 真实 capture 链路生成的
  audit_input 窗口（events 完整、球员快照取「带可观测标记的球员」子集且字段逐字保留），
  覆盖出界（sideline/goal_line）+ corner/throw_in/free_kick/clearance + dead_ball 窗口。
- `tools/fixtures/generate-real-audit-fixture.mjs`：可复现生成器（不进测试路径）。
- `detectors.test.mjs` 主输入换真实形状，保留合成边界用例（result:'out' 兼容 /
  几何出界防御 / 布尔排除位兼容）。

### P5 测试 + 审阅闭环
- 契约断言测试（`tools/detector-field-contract.test.mjs`）：
  1. 真实 fixture 上每个 `reads` 字段有生产者或登记 known-gap；`legacy_reads` 在真实数据里**从不出现**。
  2. **实现漂移守卫**：用 Proxy 记录 runAudit 真实读到的事件/快照键，断言 ⊆（reads ∪ legacy_reads
     ∪ 结构键）——契约与代码不一致即红。
- 全绿门槛：`./verify.sh` + `npx openspec validate --all --strict`。
- 审阅闭环：独立零记忆 subagent 审阅 → 修复 → 再审阅至无遗留。

## 范围纪律

- 不碰引擎 `engine/src/lib.rs`（#25）、不新增 detector（#34/#35）、不标定 ignored_interception（#36）、
  不改事件流协议字段语义。
- viewer 运行时 JS：本轮**需要**动 `viewer/derive-audit-features.js`（加 schema_version）→
  必须按项目 CLAUDE.md §4 bump `viewer/index.html` 的 `?v=` 并注明。

## 进度记录

- [x] 现状核实（含真实引擎实证 + 基线测试）
- [x] P1 契约清单模块（`tools/detector-field-contract.mjs`）
- [x] P2 L1 修断裂（D1 出界证据 / D2 排除位 / D3 字段推导 / D4 降级 / P2.6 死代码）
- [x] P3 版本保护（derive 加 version；runAudit 门；runDiagnosis 捕获成 invalid_audit_input）
- [x] P4 真实 fixture（生成器 + 88KB 7 窗口 fixture + 主输入替换）
- [x] P5.1-P5.4 测试全绿 + openspec validate
- [ ] P5.5 审阅闭环

## 实施记录（P1-P4）

### 新增文件

| 文件 | 内容 |
|------|------|
| `tools/detector-field-contract.mjs` | 字段契约清单：4 个 detector + `pass_outcomes` 的 reads/legacy_reads/producers/known_gaps；`AUDIT_INPUT_TOP_LEVEL_KEYS`；工具函数 `allowedReadKeys`/`allAllowedReadKeys`/`fieldsWithoutProducer`。`KNOWN_GAPS` 带 `kind`（unproducible/semantic/removed）。 |
| `tools/detector-field-contract.test.mjs` | 契约断言 + **实现漂移守卫**（记录型 Proxy 跑 runAudit，抓真实读键，必须 ⊆ 契约允许集）。 |
| `tools/fixtures/generate-real-audit-fixture.mjs` | 真实 fixture 生成器（真实 WASM → parseEventStream → Game.seekTo → captureObservation → audit_input）。 |
| `tools/fixtures/real-audit-input.json` | 7 个真实窗口（out_sideline/throw_in/free_kick/corner/out_goal_line/clearance/whistle_dead_ball），确定性 88KB。 |

### 修改文件

| 文件 | 改动 |
|------|------|
| `viewer/derive-audit-features.js` | 新增 `AUDIT_INPUT_SCHEMA_VERSION = 'audit-input/1'`；`deriveAuditInput` 输出带 `schema_version`。 |
| `tools/detectors.mjs` | D1 出界证据（detail 优先）/ D2 排除位（detail 集合 + 布尔兼容）/ D3 out_reason 推导、删 target_distance / D4 降级 / D6 版本门。 |
| `viewer/index.html` | `?v=` `20260912-2` → `20260912-3`（动了 viewer 运行时 JS）。 |
| `detectors.test.mjs` | 顶部 `runAudit` 注入 schema_version 包装（版本门用 `runAuditRaw` 直测）；新增真实 fixture 回归 + D3/D4/版本门用例；`formation_hold` 用例改写为「死代码已删」的行为断言。 |
| `tools/{runner,service,queue-cli,bundle}.test.mjs` | bundle fixture 补 `schema_version`；runner 新增 `invalid_audit_input` 用例。 |
| `tools/runner.mjs` | runAudit 调用包 try/catch → `finalize('failed', {failure_kind:'invalid_audit_input'})`。 |

### 关键发现（自实现 + 自查发现）

1. **D4 profile 键名不一致**：detector_id 是 `ignored_interception_opportunity`，profile 配置块是
   `ignored_interception`。直接 `profile[f.detector_id]` 永远 undefined → 降级静默失效。
   已加显式 `DETECTOR_PROFILE_KEY` 映射（不靠字符串拼）。
2. **漂移守卫真的抓到东西**：`ignored_interception` 读 `defender_id` 但契约漏登记 → 测试红。
   正是这个 change 想要的那种「测试能发现清单落后于代码」。
3. **K3 行为变化是故意的**：删 `formation_hold` 死代码后，「只带 formation_hold 的站桩」不再被豁免。
   该豁免过去只对合成 fixture 生效、对真实数据从未生效（无生产者）。用测试钉住新行为。

### 已知偏差（design.md 未明写，实现时按既有契约补齐）

- `runDiagnosis` 的 `invalid_audit_input` failure_kind 是新增的（design 只说 runAudit 抛错）。
  理由：runner 是后台编排循环，异常抛穿会连任务记录都留不下——那是另一种静默。与既有
  `bundle_read_error`/`invalid_bundle` 同模式。**待审阅确认这个补充合理**。

### 测试结果

- `./verify.sh`：viewer 288 绿 / tools 354 绿（+33 新）/ cargo 绿 / WASM e2e 绿 / realism L1 绿。
- `npx openspec validate --all --strict`：17 passed, 0 failed。
