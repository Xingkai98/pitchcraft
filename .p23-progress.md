# P23 进度：罚下球员不得再参与比赛（sent_off 全路径排除）

分支：`p23-sent-off-participation/2026-09-13`（基于 main `fb4207c`，含立项 commit `c123414`）
Issue：#34

## 现状核实（动手前通读结论）

- `sent_off: [bool; 22]`（lib.rs:570）由 `apply_card`（lib.rs:2819）置位；红牌事件（`foul[card=red]`）
  是唯一对外可见的罚下信号，`sent_off` 不进事件 JSON。
- 已有守卫：`nearest_in_team`(508) / `nearest_any`(524) / `nearest_defender_avail`(2773) / carrier 守卫(882)。
- 缺失守卫（本 change 修）：
  1. `compute_movers` 第一遍目标循环（lib.rs:1154-1155，`excluded`/`carrier` 守卫、无 sent_off）
     → 罚下球员照常算队形目标 → 第三遍（1198-1199）照常产 mover。
  2. `compute_movers` 第三遍门将分支（1201-1203）：门将走 `keeper_return`，不受 targets 约束
     → 罚下门将仍产 mover（D1 要求罚下门将也不产 keeper_return）。
  3. `pick_close_down_players`（1053）循环内只有 team/GK/carrier 守卫。
  4. `nearest_defender`（2614）只接 `&[(f64,f64)]`（无 `st`）→ 无法读 sent_off，需改签名接 `&MatchState`。
     调用点 3 个：1258（tackle 槽 same_pair 判定）、1381（拦截者选择）、1663（tackle 结算）。
- 潜在第四条路径（design 未列，待 L2 暴露再定）：`nearest_teammate`（2724）无 sent_off 守卫，
  pass 的 `to` 若命中罚下球员 → 其随后成为 carrier → 会被 D2 的 carrier/subject 不变量抓到。
  D2 的 spec 要求是「零参与」，故若 L2 抓到，需一并修（属同一语义，非方向分歧）。

## 实现计划

1. **测试先行（红）**
   - P3 L2 不变量：`MatchStats.n_sent_off_participation` + `n_sent_off_events`（诊断用）
     + `mover_ids` 提取器；`aggregate` 维护罚下集合；`l2_cross_event_invariants` 断言为 0。
     （先跑，确认红 —— 证明能抓到当前 bug）
   - P2 单元测试（lib.rs `mod tests`）：
     - `compute_movers`：`sent_off[5]=true` → movers 不含 id 5；`sent_off[0]=true` → 不产 keeper_return。
     - `nearest_defender`：罚下者更近也不被选。
     - `pick_close_down_players`：罚下者更近也不被选。
2. **P1 引擎修复**（三条路径 + 按需补 nearest_teammate）
3. **P4 golden 重基线**：先人工核对 10 seed 摘要（比分/进球/射门/传球/犯规语义数字不动）→ `ACCEPT_GOLDEN=1`
4. **P5 收尾**：`./verify.sh` 全绿 + `npx openspec validate --all --strict` + 独立 subagent 审阅闭环

## 阶段记录

- [x] 阶段 0：通读 change 四文档 + 相关引擎代码，记计划
- [x] 阶段 1：测试先行（红）——L2 抓到 seed 9 有 300 次违规，证明测试有效
- [x] 阶段 2：引擎修复（绿）——见下「实现落点」
- [x] 阶段 3：golden 重基线（人工核对后）
- [ ] 阶段 4：verify + 审阅闭环

## 实现落点（最终）

引擎修复（engine/src/lib.rs）——比 design D1 多两条（L2 不变量暴露）：

| 路径 | 位置 | 修法 |
|---|---|---|
| mover 目标 | `compute_movers` 第一遍 | `if st.sent_off[id as usize] { continue; }` |
| mover 产出（门将） | `compute_movers` 第三遍 | 同上（罚下门将不产 keeper_return） |
| transition 逼抢 | `pick_close_down_players` | 同上 |
| 抢断/拦截者 | `nearest_defender` | 签名 `&[(f64,f64)]` → `&MatchState`；循环内守卫（3 调用点更新） |
| **传球目标（design 未列）** | `nearest_teammate` | 签名同上；循环内守卫（4 调用点更新）——罚下球员不再被选为接球目标 |
| **向前传球目标（design 未列）** | `emit_forward_pass_highlight` 的"最靠前队友"循环 | 循环内守卫 |

后两条是 L2 不变量实测暴露的真路径：seed 9 罚下 id 7 后，`nearest_teammate` 仍把它选为
pass 的 `to` → 其成为 carrier（beat.main.subject）→ 继续带球/传球。属「罚下球员不得参与任何
事件」同一语义的补全，非设计方向分歧（D2 spec 的 `carrier`/`subject` 不变量明文要求零参与）。

引擎测试：
- `tests/realism.rs`：`MatchStats` 增 `n_sent_off_participation` + `sent_off_violations`；新增
  `mover_ids` 提取器；`aggregate` 从 `foul[card=red]` 重建罚下集合、逐事件检查
  subject/movers[].id/carrier/interceptor；`l2_cross_event_invariants` 断言为 0。
- `src/lib.rs` 单测 4 个：`p23_sent_off_produces_no_mover`（含未罚下对照组）、
  `p23_sent_off_keeper_produces_no_keeper_return`、`p23_sent_off_not_picked_as_tackler`、
  `p23_sent_off_not_picked_as_close_down`。

golden 重基线：
- 10 个 canary seed 中只有 seed 9 变（唯一有红牌的 seed）；其余 9 个摘要逐字段无差异。
- seed 9 摘要变化：比分/进球不变（0-0），n_beats 5369→5367、n_shot 11→8、mover/传球相关微变
  —— 都是「少一人继续跑位/接球」被移除后的连锁，非语义数字漂移。
- 旧 vs 新事件流逐条 diff：首个分歧正好是红牌事件后第一条 beat，且唯一差异是 movers 里去掉 id 7。
  `git diff engine/tests/golden/` 只动 seed-9.json。

| **开球路径（宽扫暴露）** | `emit_shot_highlight` / `emit_head_shot` 的 `kickoff_id`、`finalize_highlight` ShotGoal 的 `receiver` | 硬编码 id（home 丢球开球 9 / away 丢球 12；接球 10/11）→ 改用新增 `kickoff_pick(st, default_id)`：默认者未被罚下恒返回默认（不改既有流），被罚下则按 id 顺序取该队下一个未被罚下者。`advance_dead_ball` preparing 分支手动 push 的 kickoff mover 因此不再驱动罚下球员 |

开球路径是**第二个 design 未列、且 15 seed 窗口守不住**的洞：宽扫 1..=2000（564 张红牌）暴露 4 个
seed（260/884/1271/1658）里罚下者恰是硬编码开球者/接球者，会以 `subject`/`carrier` 重新入场。

## 验证结果

- `./verify.sh` 全绿（引擎 59 单测 + L2/golden + viewer 297 + tools 385 + WASM e2e + L1 统计）。
- `npx openspec validate --all --strict --type change` 17/17 通过。
- 2000 seed 宽扫（seed 1..=2000，564 张红牌）：违规 seed 0。
- 新增 `l2_sent_off_kickoff_seeds` 定向守 4 个开球 seed（用 afade42 版引擎验证会红）。

## 审阅闭环记录

codex（gpt-5.6-sol）独立审阅：**不通过**，3 条 P1。逐条核实：

- **P1-1 L2 漏检 `to`** —— 成立，已修（参与者检查补 `to`；补后 L2 仍绿）。
- **P1-2 退化态 panic / kickoff_pick 兜底返回罚下者** —— 成立，已修。每队最多 10 红（门将不产犯规）
  → 10 外场全罚下是规则可达边界；我加的 sent_off 过滤把原 `unwrap()` 推到会 panic 处。已让
  `nearest_defender`/`nearest_teammate`/`kickoff_pick` 在退化态回退门将（恒不被罚下），并加单测。
- **P1-3 seed-9 golden 漂移超「仅移除参与」** —— drift 真实，但经逐事件溯源确认是修复的**必然连锁**、
  非语义漂移，不接受「隐藏回归」定性。证据：
  - 首个分歧 = 红牌后第一条 beat，唯一差异是 movers 去掉 id 7；
  - 下一处分歧 3395s：旧流 `interceptor=7`（罚下者仍在断球）→ 新流正确改 `interceptor=8`；
  - 拦截点=拦截者位置（`intercept_pass_highlight` 把球置 interceptor 脚下）→ 8/7 位置不同
    → 后续传球链/球权/时序全改道 → 2000+ 下游事件重排，射门/传球/犯规计数随之变化；
  - 比分/进球（0-0）不变。n_shot 11→8 等是链式改道结果，非独立语义漂移。

**残留跟进（非本 change 范围）**：`nearest_in_team` 在无合法候选时返回 `-1`，其调用点
（`advance_restart_prep`/`start_loose_ball`/角球 battle 等）会索引 `st.pos[-1 as usize]` → 越界 panic。
这是该函数**原有**行为（P23 之前就存在），只在「某队外场全部罚下」这一规则可达边界触发。
本次已把同一退化态下我新引入的选择器（nearest_defender/nearest_teammate/kickoff_pick）total 化，
未扩围改 nearest_in_team——建议单开 issue 处理（与 #34 同源但独立）。
