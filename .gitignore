# Rust 构建产物
engine/target/
# WASM 产物（由 cargo build 生成）
viewer/engine.wasm
# 编辑器
.DS_Store
*.swp
# 本地诊断凭证（.env 仅本机使用；.env.example 是提交的模板，不受此规则影响）
.env

# 本地诊断问题数据（P10-P13 problem/task/bundle，运行期生成，不入库）
.scratch/tasks/

# Node 依赖（仅测试用 devDependency，如 jsdom；不进运行时/WASM/浏览器 bundle）
node_modules/

# 真实比赛对照数据（由 tools/convert-tracking-to-frames.mjs 从公开数据集生成；
# 源数据 60MB+/场、转换产物 18MB/场，不入库。生成方式见 .scratch/notes/real-match-reference.md）
# 例外：P36 比赛标尺的基线摘要（数百字节，tools/benchmark-baseline.mjs 生成）必须入库——
# 它是「真实观测范围」的冻结记录，不入库则对比工具无从对照。
# 用 `data/*` 而非 `data/`：目录整体排除后 git 不会进目录，文件级 `!` 例外无法生效。
viewer/data/*
!viewer/data/benchmark-baseline.json
# 公开 tracking 数据集的本地副本（下载脚本落这里）
.scratch/tracking-data/
