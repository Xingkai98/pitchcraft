# P29 进度：射门机会迁移（#25 阶段 2B）

分支 `p29-shot-opportunity-migration/2026-09-14`（立项 `1a68250`）。
方案定稿：`openspec/changes/p29-shot-opportunity-migration/{proposal,design,tasks}.md` +
`reviews/grill-design.md`（Q1-Q4 已拍板；P1-P5 任务表）。

## 勘察结论（动手前）

- 2A/P28 已就位：`CarrierAction/DefensiveAction/ActionResolution/CarrierPlan/ActionPlan` +
  `evaluate_carrier_action` / `evaluate_defensive_action` / `resolve_action_opportunity` /
  `build_action_plan` / `execute_action_resolution`（`engine/src/lib.rs:576-1446`）。
- 射门当前有 **3 个绕过 hazard 的直射点**（2B 必须全部收口到起脚窗口）：
  1. `advance_shot_setup` 的 `dist <= target || ticks_left == 0` → 到即射（`:2488`）。
  2. `evaluate_fallback_carrier_action` 的 `dist <= target` 分支 → `CarrierExecution::Shoot`（`:1144`）。
  3. `finalize_highlight` 的 `shot_pending_after_pass` + `dist <= target` 直射（`:2645`）。
- 事件协议无需改：D3 的 `canceled` 走**内部状态取消**（`shot_setup = None`），对外只有
  tackle → loose ball（proposal「不改事件协议」+ 非目标「不改 viewer」）。
- `model_version` 只用于 golden 目录选择（`realism.rs:955`），无 viewer/wasm 耦合 → 升 3 安全。

## 实现计划

### P1 hazard 打分纯函数（先红后绿）

- `ShotOpportunityFeatures { distance_m, angle_cos, nearest_defender_m, second_defender_m, cooldown_ticks }`
- `shot_angle_cos`：持球者→所攻球门中心（真实米，含 y 角向）× 进攻方向夹角的余弦；
  ≤ 0 = 背向/平行门线 → `facing_goal() == false` → **禁止 Shoot 候选**（D2）。
- `shot_distance_quality`：分段复用 shot_bucket 边界（6-16.5→0.9-1.0 / 16.5-25→0.45-0.9 /
  25-35→0.05-0.45 / >35→0-0.05），线性内插。
- `shot_space_score(d)`：≤2m→0、2-8m 线性、≥8m→1；`space_available = 0.65*nearest + 0.35*second`。
- `defensive_pressure = 0.65*near_scale + 0.35*second_scale`（尺度 `SHOT_PRESSURE_*_M`，米）。
- `cooldown_penalty = shot_cooldown_ticks / SHOT_COOLDOWN_TICKS`（**局部衰减计时器**，
  非「本场射门计数」——C 路原则）。
- `compute_shot_score = BASE + GAIN * (distance + angle + space - pressure - cooldown)`；
  `shot_hazard_probability(score, window) = 1 - exp(-exp(score) * window)`。
- **关于 GAIN**：grill 定稿的公式形状不含增益常数。实测推演表明：五因子各自量纲 ∈ [0,1]，
  因子和的全幅仅 ~5 nats，不足以同时做到「好机会 p 高」与「差机会 p 低」（需要 ~4 nats 的
  *区分度* + 居中）。故按同一函数族 `hazard = e^BASE · e^(GAIN·Σ)` 引入增益（等价于 log 尺度
  缩放），BASE/GAIN 是**标定常数**，不是公式形状改动。数值在实现后按实测频率标定，记录于下。

### P2 起脚窗口 + committed

- `ShotSetup` 增 `in_window: bool` / `window_ticks: u32` / `committed: bool`。
- `advance_shot_setup` 拆两相：
  - **推进相**（未进窗口）：与现状逐 tick 同构（只沿 x 推进、零新增 RNG）；到达
    `dist <= target` 或步数耗尽 → **进入窗口**（发一拍 main beat），**不射**。
  - **窗口相**：每 tick 走统一模块 `build_shot_window_plan`：
    1. hazard 打分（每 tick 重算几何）+ 掷定 → 命中 = `committed = true` → 立即产 Shot，
       **跳过本 tick 防守评估**（提交后不可回溯，D1）。
    2. 未提交 → 防守侧评估（贴身 + 积极性 → Tackle）→ `InterruptedByTackle` →
       `shot_setup = None`（内部 canceled）→ tackle 事件（→ 成败决定松散球）。
    3. 都没有 → 窗口计时 +1；耗尽 → **转**（放弃射门，继续带球 beat）。
  - **收口**：删掉 3 个直射点，`CarrierExecution::Shoot` 只由窗口提交产生（唯一生产者）。
- `OpportunityTrigger::ShotWindow` 加入 2A 的模块（`evaluate_carrier_action` /
  `evaluate_defensive_action` 各加一臂），tally 复用 `carrier_shoot` / `defensive_tackle` /
  `res_carrier_shoot` / `res_interrupted_tackle` / `exec_shoot` / `exec_tackle`。

### P3 被抢断打断

- 窗口内抢断：`nearest_defender` 距离 ≤ `TACKLE_DISTANCE_THRESHOLD_METERS` 且 `should_tackle`
  → `Tackle{same_pair, far=false}`（阈值内故 far 恒 false）。
- 抢断**成败都打断射门序列**（tackle 高亮占用该 tick，起脚节奏丢失）；成败只决定球权去向
  （success → 松散球；fail → 留在原持球者）。这条解释不在 grill 覆盖范围内，记录在案。
- 推进相不设抢断（Q1 否决了「推进全程可抢断」的 C 案）。

### P4 golden v3 + 频率方向性

- `MODEL_VERSION = 3`，`golden_dir(3) = "tests/golden-v3"`；v1/v2 目录保留不覆盖（已确认三个目录并存）。
- `gm_canary_seeds` 自动落 v3；`gm_v1_regression_counts_unchanged`（断言 v1 计数 == 当前）
  在 2B 必然失效 → 改为 `gm_legacy_baselines_preserved_and_differs`（v1/v2 基线存在且自洽、
  v1-vs-v2 仍满足 P27 关系、当前流与两者都不同 = 真重基线）。
- 频率断言改方向性（D4）：把 `shots_per`、`shot/tackle` 的注释从「槽位配额」改为「经验体量带」，
  并指明方向性断言在引擎内（`p29_window_commit_rate_falls_with_pressure` /
  `p29_hazard_factor_directions`）。
- `l2_sent_off_kickoff_seeds` 的 4 个钉死 seed（260/884/1271/1658）在 2B 后不再含红牌（RNG 序变）
  → 按当前引擎重扫（1..=2000，523 张红牌）取「红牌 + 进球」的 seed 钉死：2 / 5 / 18 / 52。

### P5 测试 + 收尾

- 纯函数方向测试（近门/正对/无压 → 高；远射/边路/贴身 → 低；背向 → 禁止候选；冷却局部性）。
- 起脚窗口测试（到射程不立即射 / committed 不可回溯 / 窗口内被抢断不产 Shot / 窗口是唯一射门路径）。
- `./verify.sh` 全绿 + `openspec validate --all --strict` 17/17。
- paseo 零记忆 subagent 审阅闭环。

## 标定记录（hazard 数值，实现后按实测确定）

- `BASE_SHOT_TENDENCY = -4.0`、`SHOT_FACTOR_GAIN = 2.5`：唯一同时保住 L1/L3 全部统计带的标定点。
  扫描：BASE=-4.5 / -5.0 时 `l1_shot_result_distributions` 或 `l3_shot_ratios` 变红——降低起脚率会
  把桶占比挤出 L1 带（射门少 → 各桶样本偏移）。故 2B 的起脚率变化幅度受既有 L1 带约束。
- 实测（200 seed × 90min，SEEDS_L1 窗口）：shot 6.94/场（基线 7.07）、转化 13.8%、射正 36.3%、
  禁区内占比 57.9%、tackle 4.67/场、shot/tackle 1.49 —— 全部落 L1/L3 带。
- 起脚窗口实测：进 7.00/场、提交 6.80、被抢断 0.17、转 0.03（提交率 0.972）；压迫方向成立
  （无压提交率 > 贴身）。**窗口提交率高**是设计信号：推进到射程的机会多已摆脱防守 → hazard 高，
  这与「射门是槽强制的」区别在**进入窗口本身由几何决定**（entry 门 + 距离/压迫共同筛选），
  而非由槽位 roll 决定。

## 已知张力（记录在案，非缺陷）

1. **距离方向门改用压迫维度**：起脚窗口的距离方向被联合分布混淆（远射窗口只在突围时出现 →
   远射桶样本偏无压、提交率反而不低，实测近门 99.3% vs 远射 100%）。D2 的 `distance_quality`
   因子方向由纯函数控制变量测试（`p29_hazard_factor_directions`）钉死；真实数据上的方向门用
   不受混淆的 `defensive_pressure` 维度。这是 D4「断言方向而非配额」的正确落法。
2. **抢断成败都取消射门**：D3 文本是「被抢断 → canceled → tackle」，未区分成败；实现取
   「成败都取消」（tackle 高亮占用 tick，起脚节奏丢失），成败只决定球权去向。这是对 D3 的具体化，
   若用户要「仅抢断成功才取消」，需改 `shot_window_plan`（属设计级，已在下方「待确认」标注）。
3. **`p7_frequency_5min` 的 5min 窗口加宽 20→100 seed**：非调目标，是修统计噪声——5min 进球
   是低均值计数（真实率 ~0.65），20 seed 下旧窗口实测 1.05 而长程真值 0.72（偏离 45%），断言
   实际靠运气。加宽后实测 0.62/场（> 0.5 门槛），90min 侧保持 20 seed 控成本。

## 待确认（设计级，未自行改公式/阈值）

1. **抢断成败都取消射门**：D3 文本「被抢断 → canceled → tackle」未区分成败；实现取「成败都取消」
   （tackle 高亮占用 tick，起脚节奏丢失），成败只决定球权去向。已按文本最自然读法实现并记录；
   如需改为「仅成功取消」，属语义级改动，未擅自改。
2. **冷却因子在当前标定下近乎惰性**：`SHOT_COOLDOWN_TICKS = 8`，但实测射门间隔 ~700 tick →
   窗口进入时冷却几乎总已归零（`shot_cooldown_feature_max == 0`）。因子已正确接线（射门后重置、
   每 tick 衰减、执行层守卫钉死值域与重置计数），只是当前标定下几乎不影响概率。
   若要让冷却真正参与起脚频率，需调 `SHOT_COOLDOWN_TICKS`（系数级，未擅自改）。

## 审阅闭环

### 第一轮（paseo 独立零记忆 agent `3a9aaf92`）：**有遗留 P0×1 + P1×3**

独立复现：9 项审阅重点全部验证。变异测试 M1/M2/M5/M8/M9 自建并复现。结论性通过项：
五因子方向（控制变量）、committed 不可回溯（结构性）、被抢断不产 Shot（M9 双红）、golden v3
真重基线（v1/v2 diff 为空）、C 路实现侧合规、射门唯一生产者、tally 分区等式（2109=2048+53+8）、
`./verify.sh` 全绿、openspec 17/17。

发现与修复：
- **P0-1（归档静默丢 scenario）**：delta 的 `## MODIFIED` 只写 4 个新 scenario → 归档会丢主 spec
  该 requirement 的 4 个既有 scenario + 失效契约句（validate 17/17 看不见，= memory
  `openspec-archive-validate-gap` 记录的坑）。修：完整复制 4 个既有 scenario + 契约句；
  /tmp 试归档确认 8 个 scenario 全在。
- **P1-1（死冷却因子）**：`shot_cooldown_ticks` 只读不写 → 五因子实为四因子。修：改剩余倒计时，
  tick 衰减 + 射门重置。
- **P1-2（C 路无执行层守卫）**：原测试只测纯函数构造的 feature；M8 变异（输入源换全场累计量）
  穿全部默认测试。修：跑 200 场、执行层断言冷却值域 + 重置计数；M8 实测变红。
- **P1-3（空洞测试）**：`p29_committed_is_irreversible` 防守者摆 97m 外，删 committed 短路仍绿。
  修：控制对照（同几何贴身 0m，比较 committed T/F）；M5 实测变红。
- **P2-1/P2-2（死计数器/死字段）**：删 `shot_window_commits_by_bucket`、`approach_ticks`；
  `shot_window_holds` 加 `> 0` 断言。
- **P2-3/P2-5/P2-6**：背向分支注释「结构性不可达」（300 场探针 nonfacing=0）；抢断 scenario
  区分成败措辞；方向门加 margin(0.10)，让断言**自身**绑定 pressure 因子（M4 实测 gap 0.021 → 红）。
- **P2-4**：spec 公式补 `gain` 标定常数说明。

### 第二轮（同一零记忆 agent 复核 `81e8013`）：**四项修复全部确认到位，无新遗留 P0/P1**

逐项变异/归档复现（全部由审阅者独立重做）：
- **P0-1**：/tmp 试归档 → 主 spec 该 requirement 有 **8 个** scenario（4 既有回来 + 4 新），
  契约句与优先级括号都在；scenario 总数 53 → 57（+4，与新增一致）；归档后 validate 16/16。
- **P1-1**：`shot_cooldown_ticks` 现有两条写路径（tick 衰减 / 射门重置）——死字段判定不再成立。
- **P1-2**：重做 M8（输入源换 `shot_window_commits`，忠于违规语义不钳制）→
  `p29_cooldown_is_local_not_match_wide` **红**（值上限 9 > 8）；上轮同变异全绿。
- **P1-3**：重做 M5（删 committed 短路）→ `p29_committed_is_irreversible` **红**；上轮绿。
- **P2-6**：重做 M4（删 `defensive_pressure`）→ 方向门 **红**（gap 0.021 ≤ 0.10）；实测真实
  gap 0.30（1.000 vs 0.699），阈值 0.10 约 3× 余量。

**新引入问题排查（未发现新 P0/P1）**：按结算分派的 `_` 分支只承接 `NoAction`（ShotWindow 防守
侧只返回 Tackle|None，持球侧只 Shoot|AwardDeadBall|None，四类分辨率穷尽）；确定性绿 + golden-v3
逐字节未变；`resets == commits`（头球既不重置也不计 commits → 不破坏等式，变异 M14 验证）；
分区等式 worst_delta=0（M11/M15 背向变异触发红）。

**第二轮新发现（全 P2，已修 2 条）：**
- P2-1 冷却因子真实比赛中仍恒 0（标定使然，已记「待确认②」）——文档化，不阻塞。
- P2-2 测试 doc comment 第 3 条与实际断言不符 → 已删该条（改为指向 `p29_cooldown_feature_is_live`）。
- P2-3 分区等式 ±1 容差可掩盖「每场恰丢 1 终局」→ **已收紧**：容差只在「确实有 `in_window`
  的残留窗口」时允许（绑可观测条件而非无条件 ±1）。**实测**：断言采样点（终场）`dangling`
  恒 0（300 seed）→ 收紧后等式在该测试里实际等价于精确的 `ends == entries`；而 `dangling`
  在比赛**进行中**是活跃条件（tick 级探针：300 场中 2166 个 tick 为 1）。**有效性已验证**：
  变异 M17（每场恰丢 1 终局）在收紧式下红、在旧 ±1 式下绿——收紧是实质的，不是换写法。

## 进展

- [x] 勘察 + 计划
- [x] P1 hazard 纯函数
- [x] P2 起脚窗口 + committed
- [x] P3 被抢断打断
- [x] P4 golden v3 + 频率方向性
- [x] P5 测试 + verify 全绿
- [x] P5 审阅第一轮（P0×1 + P1×3 全修）
- [x] P5 审阅第二轮（四项全确认 + 2 条 P2 收紧）→ **无遗留 P0/P1**
- [x] P5 审阅第三轮窄幅确认（两条 P2 确认；收紧容差经 M17 变异验证有效）→ **审阅闭环通过**

